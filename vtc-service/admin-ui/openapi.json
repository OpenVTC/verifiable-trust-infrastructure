{
  "openapi": "3.1.0",
  "info": {
    "title": "Verifiable Trust Community (VTC) API",
    "description": "Community lifecycle, ACL, audit, policy, credentials, endorsements, and cross-community recognition REST surface of a Verifiable Trust Community.",
    "contact": {
      "name": "Glenn Gore",
      "email": "glenn@affinidi.com"
    },
    "license": {
      "name": "Apache-2.0",
      "identifier": "Apache-2.0"
    },
    "version": "0.11.58"
  },
  "paths": {
    "/v1/acl": {
      "get": {
        "tags": [
          "acl"
        ],
        "summary": "GET /acl — list ACL entries visible to the caller. Auth: Manage.",
        "operationId": "list_acl",
        "parameters": [
          {
            "name": "role",
            "in": "query",
            "description": "Return only entries with this role.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "scope",
            "in": "query",
            "description": "Return only entries carrying this scope (canonical name for\nwhat VTC stores as an allowed context).",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "subjectPrefix",
            "in": "query",
            "description": "Return only entries whose subject starts with this prefix.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "description": "Page size. Clamped to `1..=200`. Defaults to 50.",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 0
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Opaque continuation token from a previous page's `cursor`.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Visible ACL entries",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AclListResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller lacks manage authority"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "post": {
        "tags": [
          "acl"
        ],
        "summary": "POST /acl — create a new ACL entry. Auth: Manage.",
        "operationId": "create_acl",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateAclRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "ACL entry created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AclEntryEnvelope"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller lacks manage authority"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/acl/{did}": {
      "get": {
        "tags": [
          "acl"
        ],
        "summary": "GET /acl/{did} — retrieve a single ACL entry. Auth: Manage.",
        "operationId": "get_acl",
        "parameters": [
          {
            "name": "did",
            "in": "path",
            "description": "Subject DID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "ACL entry",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AclEntryEnvelope"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller lacks manage authority"
          },
          "404": {
            "description": "ACL entry not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "delete": {
        "tags": [
          "acl"
        ],
        "summary": "DELETE /acl/{did} — revoke: remove the entry, or reduce its scopes\nwhen `scopes` is supplied. Auth: Admin.",
        "operationId": "delete_acl",
        "parameters": [
          {
            "name": "did",
            "in": "path",
            "description": "Subject DID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "ACL entry deleted"
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "ACL entry not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "patch": {
        "tags": [
          "acl"
        ],
        "summary": "PATCH /acl/{did} — modify an ACL entry. Auth: Admin.",
        "operationId": "update_acl",
        "parameters": [
          {
            "name": "did",
            "in": "path",
            "description": "Subject DID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateAclRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Updated ACL entry",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AclEntryEnvelope"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "ACL entry not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/admin/bootstrap": {
      "post": {
        "tags": [
          "admin"
        ],
        "operationId": "bootstrap",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/BootstrapRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "First admin bootstrapped; community installed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BootstrapResponse"
                }
              }
            }
          },
          "409": {
            "description": "An admin already exists"
          }
        }
      }
    },
    "/v1/admin/config": {
      "get": {
        "tags": [
          "admin"
        ],
        "summary": "GET handler.",
        "operationId": "get_config",
        "responses": {
          "200": {
            "description": "Four-layer-merged effective config",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EffectiveConfig"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "patch": {
        "tags": [
          "admin"
        ],
        "summary": "PATCH handler.",
        "operationId": "patch_config",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PatchRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Applied / pending-restart / rejected keys",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PatchResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/admin/config/export": {
      "post": {
        "tags": [
          "admin"
        ],
        "operationId": "export_config",
        "responses": {
          "200": {
            "description": "Portable config + community-profile export",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ExportResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/admin/config/import": {
      "post": {
        "tags": [
          "admin"
        ],
        "operationId": "import_config",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ImportRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Import diff (preview) or applied changes",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ImportResponse"
                }
              }
            }
          },
          "400": {
            "description": "Document carries an unsupported schemaVersion"
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "409": {
            "description": "Document was taken from a different community"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/admin/config/reload": {
      "post": {
        "tags": [
          "admin"
        ],
        "summary": "`POST /v1/admin/config/reload` handler. Re-reads the\n`EffectiveConfig` and diffs against the live in-memory config;\nfor each hot-reloadable key whose effective value differs, the\nin-memory `AppConfig` is updated. Emits `ConfigReloaded` listing\nthe keys that actually changed.",
        "description": "**Phase 0 limitation**: only the Phase-0 registry's\nhot-reloadable keys (`log.level` today) are propagated. Future\nruntime-state subscribers (tracing subscriber filter handle,\nsession-cleanup interval, etc.) will plug into the same diff\nloop.",
        "operationId": "reload_config",
        "responses": {
          "200": {
            "description": "Keys re-applied in-memory",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReloadResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/admin/config/restart": {
      "post": {
        "tags": [
          "admin"
        ],
        "summary": "`POST /v1/admin/config/restart` handler.",
        "description": "Refuses (`412 Precondition Failed`,\n`SupervisorRequired`) unless a supervisor is detected — restart\nwithout an external supervisor is just \"kill the process\" and a\ncaller asking for `restart` likely means \"have the daemon come\nback up afterwards\". Detection lives in\n[`crate::supervisor::detect_supervisor`].\n\nOn success the handler emits `RestartRequested` to the audit\nlog *before* signalling shutdown — so the row survives even if\nthe drain wedges.",
        "operationId": "restart_config",
        "responses": {
          "200": {
            "description": "Restart requested",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RestartResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/admin/invites": {
      "get": {
        "tags": [
          "admin"
        ],
        "operationId": "list_invites",
        "responses": {
          "200": {
            "description": "Outstanding + terminal install-token invites",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListInvitesResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "post": {
        "tags": [
          "admin"
        ],
        "operationId": "create_invite",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateInviteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Install URL + one-time claim code minted",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CreateInviteResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "409": {
            "description": "Target DID already has a non-admin ACL grant"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/admin/invites/{jti}": {
      "delete": {
        "tags": [
          "admin"
        ],
        "operationId": "revoke_invite",
        "parameters": [
          {
            "name": "jti",
            "in": "path",
            "description": "Invite token id (jti)",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Invite revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RevokeInviteResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "Invite not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/admin/passkeys": {
      "get": {
        "tags": [
          "admin"
        ],
        "operationId": "adminPasskeyList",
        "responses": {
          "200": {
            "description": "Registered admin passkeys",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "No admin entry for caller"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/admin/passkeys/register/finish": {
      "post": {
        "tags": [
          "admin"
        ],
        "operationId": "register_finish",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterFinishRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "New passkey registered",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterFinishResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing/invalid bearer token, or step-up UV failed"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/admin/passkeys/register/start": {
      "post": {
        "tags": [
          "admin"
        ],
        "operationId": "register_start",
        "responses": {
          "200": {
            "description": "New-device registration + step-up UV challenges",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterStartResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "No passkey user for caller"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/admin/passkeys/revoke/finish": {
      "post": {
        "tags": [
          "admin"
        ],
        "operationId": "revoke_finish",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RevokeFinishRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Passkey revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RevokeFinishResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing/invalid bearer token, or step-up UV failed"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "credential_id not registered for this admin"
          },
          "409": {
            "description": "Refusing to leave the admin with zero passkeys"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/admin/passkeys/revoke/start": {
      "post": {
        "tags": [
          "admin"
        ],
        "operationId": "revoke_start",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RevokeStartRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Step-up UV challenge for the revocation",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RevokeStartResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "credential_id not registered for this admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/audit": {
      "get": {
        "tags": [
          "audit"
        ],
        "summary": "GET /audit — newest-first paginated audit envelopes. Auth: Super-admin.",
        "operationId": "list_audit",
        "parameters": [
          {
            "name": "from",
            "in": "query",
            "description": "Return only entries recorded at or after this time.",
            "required": false,
            "schema": {
              "type": "string",
              "format": "date-time"
            }
          },
          {
            "name": "to",
            "in": "query",
            "description": "Return only entries recorded strictly before this time.",
            "required": false,
            "schema": {
              "type": "string",
              "format": "date-time"
            }
          },
          {
            "name": "action",
            "in": "query",
            "description": "Return only entries whose `action` equals this value — the\nevent variant name, e.g. `MemberRemoved`.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "actor",
            "in": "query",
            "description": "Return only entries whose actor DID equals this value. Matches\nthe plaintext, so RTBF-redacted rows are never returned by an\nactor filter (canonical requires exactly this).",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "outcome",
            "in": "query",
            "description": "Not supported by this maintainer — see [`unsupported_filters`].",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "contextId",
            "in": "query",
            "description": "Not supported by this maintainer — see [`unsupported_filters`].",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "description": "Page size. Clamped to `1..=200`. Defaults to 50.",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 0
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Pagination cursor (returned by a previous call).",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated audit envelopes",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AuditListResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not a super-admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/audit/verify": {
      "get": {
        "tags": [
          "audit"
        ],
        "summary": "GET /audit/verify — verify the audit hash chain. Auth: Super-admin.",
        "description": "Walks the whole audit keyspace in ascending (chronological) key\norder and folds it through [`ChainVerifier`], so memory stays\nconstant regardless of log size.\n\n**What a `verified: true` does and does not mean.** The chain\nlinks each envelope to its predecessor, so a reorder, drop, or\nduplicate is detected. It is *not* a signature: `chain_digest` is\nan unkeyed SHA-256, so an adversary with write access to the store\ncan forge a suffix and restamp every envelope after it, and a\ntruncation to a valid prefix is indistinguishable from a quiet\nperiod.\n\nThat is what the `checkpoints` block closes (#708). Read it as the\nload-bearing half of this response: `verified: true` with\n`checkpoints.status: \"truncated\"` means the surviving log is internally\nconsistent *and* provably shorter than the community key attested to —\ni.e. exactly the attack the chain alone cannot see. See\n`docs/05-design-notes/vtc-audit-checkpoints.md`.",
        "operationId": "verify_audit_chain",
        "responses": {
          "200": {
            "description": "Chain verification result",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VerifyResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not a super-admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/auth/": {
      "post": {
        "tags": [
          "auth"
        ],
        "summary": "`POST /v1/auth/` — verify a DIDComm/SIOP/Trust-Task authentication\ndocument and issue access + refresh tokens. Unauthenticated.",
        "operationId": "authenticate",
        "requestBody": {
          "description": "DIDComm envelope, SIOP id_token envelope, or Trust-Task auth document",
          "content": {
            "text/plain": {
              "schema": {
                "type": "string"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Access + refresh tokens",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AuthenticateResponse"
                }
              }
            }
          },
          "401": {
            "description": "Authentication failed (bad proof, challenge mismatch, or replay)"
          }
        }
      }
    },
    "/v1/auth/admin-session": {
      "post": {
        "tags": [
          "auth"
        ],
        "operationId": "admin_session",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AdminSessionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Admin session + CSRF cookies set",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AdminSessionResponse"
                }
              }
            }
          },
          "401": {
            "description": "Invalid or expired access token"
          }
        }
      }
    },
    "/v1/auth/challenge": {
      "post": {
        "tags": [
          "auth"
        ],
        "summary": "Thin dispatcher — every substantive concern (ACL, rate\nlimit, session persistence) lives in the canonical handler.",
        "operationId": "authChallenge",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ChallengeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "DID-auth challenge nonce",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChallengeResponse"
                }
              }
            }
          },
          "401": {
            "description": "ACL gate rejected the subject DID"
          }
        }
      }
    },
    "/v1/auth/passkey-login/finish": {
      "post": {
        "tags": [
          "auth"
        ],
        "summary": "`POST /v1/auth/passkey-login/finish` — verify the WebAuthn assertion and\neither mint a cookie session + bearer token (`purpose: login`) or elevate\nthe caller's existing session (`purpose: stepUp`). Which one it is was\ndecided at `start`; the client does not get to re-declare it here.",
        "operationId": "passkey_login_finish",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PasskeyLoginFinishRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Access + refresh tokens (sets admin session + CSRF cookies), or the elevated session for a step-up"
          },
          "401": {
            "description": "Passkey assertion verification failed or credential not registered"
          }
        }
      }
    },
    "/v1/auth/passkey-login/start": {
      "post": {
        "tags": [
          "auth"
        ],
        "summary": "`POST /v1/auth/passkey-login/start` — issue a WebAuthn assertion\nchallenge. Unauthenticated for `purpose: login`; requires a session for\n`purpose: stepUp`.",
        "operationId": "passkey_login_start",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/PasskeyLoginStartRequest"
                  }
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "WebAuthn assertion challenge",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PasskeyLoginStartResponse"
                }
              }
            }
          },
          "401": {
            "description": "WebAuthn not configured, no passkeys registered, or step-up requested without a session"
          }
        }
      }
    },
    "/v1/auth/recognise": {
      "post": {
        "tags": [
          "recognise"
        ],
        "summary": "`POST /v1/auth/recognise` — cross-community session mint from a\nholder-signed VP embedding a foreign VEC + VMC.",
        "operationId": "recognise",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RecogniseRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Minted cross-community session",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RecogniseResponse"
                }
              }
            }
          },
          "403": {
            "description": "Holder-binding, recognition gate, or role-mapping denied"
          }
        }
      }
    },
    "/v1/auth/recognise/challenge": {
      "post": {
        "tags": [
          "recognise"
        ],
        "summary": "`POST /v1/auth/recognise/challenge` — issue a single-use, TTL'd nonce the\nholder binds into their recognise VP. Bound to this VTC's DID as the\naudience, so the resulting VP can't be replayed against a different VTC.",
        "operationId": "recognise_challenge",
        "responses": {
          "200": {
            "description": "Single-use recognition nonce",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RecogniseChallengeResponse"
                }
              }
            }
          }
        }
      }
    },
    "/v1/auth/refresh": {
      "post": {
        "tags": [
          "auth"
        ],
        "summary": "`POST /v1/auth/refresh` — exchange the presented refresh\ntoken for a new access + refresh pair.",
        "description": "Returns the canonical `AuthenticateResponse { session, tokens }`\nshape (replaces the legacy `{ sessionId, data: { accessToken,\naccessExpiresAt } }`). The full token-rotation logic — atomic\nclaim, refresh-expiry check, ACL re-look-up, AAL preservation\nacross rotation, RFC 6749 §10.4 rotation semantics — lives in\nthe canonical handler in vti-common.\n\nTwo request shapes, tried in order:\n\n1. **Trust-Task `auth/refresh/0.1` document** (canonical REST) — no\n   mediator, no DIDComm stack. See [`try_refresh_trust_task`].\n2. **DIDComm authcrypt envelope** — for clients already on a mediator.\n   Unchanged, and still gated by `bind_authcrypt_sender`.",
        "operationId": "refresh",
        "requestBody": {
          "description": "DIDComm envelope or Trust-Task refresh document",
          "content": {
            "text/plain": {
              "schema": {
                "type": "string"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Rotated access + refresh tokens",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AuthenticateResponse"
                }
              }
            }
          },
          "401": {
            "description": "Refresh token not found, revoked, or already used"
          }
        }
      }
    },
    "/v1/auth/sessions": {
      "get": {
        "tags": [
          "auth"
        ],
        "summary": "`GET /v1/auth/sessions` — list active sessions visible to the caller.\nSuper-admin sees all; context-admin sees only sessions in their contexts.",
        "operationId": "session_list",
        "responses": {
          "200": {
            "description": "Active sessions",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/SessionSummary"
                  }
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin/initiator"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "delete": {
        "tags": [
          "auth"
        ],
        "summary": "`DELETE /v1/auth/sessions?did=X` — revoke all sessions for a DID.\nSuper-admin unrestricted; context-admin limited to visible DIDs.",
        "operationId": "revoke_sessions_by_did",
        "parameters": [
          {
            "name": "did",
            "in": "query",
            "description": "Subject DID whose sessions to revoke",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Sessions revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RevokeByDidResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller cannot revoke sessions for this DID"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/auth/sessions/{session_id}": {
      "delete": {
        "tags": [
          "auth"
        ],
        "summary": "`DELETE /v1/auth/sessions/{session_id}` — revoke a single session\n(caller's own, or any if admin).",
        "operationId": "revoke_session",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "Session identifier",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Session revoked"
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Cannot revoke another user's session"
          },
          "404": {
            "description": "Session not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/auth/sign-out": {
      "post": {
        "tags": [
          "auth"
        ],
        "summary": "`POST /v1/auth/sign-out` — revoke the caller's session and\nexpire the cookie pair. The cookies' HttpOnly flag means JS\ncan't clear them itself — only the server can issue\n`Set-Cookie: ...; Max-Age=0` to delete from the browser's jar.",
        "operationId": "sign_out",
        "responses": {
          "204": {
            "description": "Session revoked and session/CSRF cookies cleared"
          },
          "401": {
            "description": "Missing or invalid bearer token"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/auth/whoami": {
      "get": {
        "tags": [
          "auth"
        ],
        "summary": "`GET /v1/auth/whoami` — returns the caller's identity claims\npulled from the access token. Lets browser SPAs render a\n\"signed in as\" indicator without exposing the JWT to JS (the\nsession cookie is HttpOnly by design).",
        "operationId": "whoami",
        "responses": {
          "200": {
            "description": "Caller identity claims",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WhoamiResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/ceremonies": {
      "get": {
        "tags": [
          "ceremonies"
        ],
        "summary": "`GET /v1/ceremonies` — list the ceremony manifests. Authenticated\n(any session); the payload is admin-UI metadata, not secret, but\nthe surface lives behind the same gate as the rest of the API.",
        "operationId": "ceremonyList",
        "responses": {
          "200": {
            "description": "Ceremony manifests",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CeremonyListResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/community/profile": {
      "get": {
        "tags": [
          "community"
        ],
        "summary": "GET handler. Returns the singleton profile + the live\ntrust-registry status.\nGET /community/profile — full community profile + live registry status.\nAuth: any authenticated session.",
        "operationId": "get_profile",
        "responses": {
          "200": {
            "description": "Community profile + registry status",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CommunityProfileResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "404": {
            "description": "Community profile not initialised"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "put": {
        "tags": [
          "community"
        ],
        "summary": "PUT handler. Admin-only. Refuses changes to `community_did`.",
        "description": "Emits a `CommunityProfileUpdated` audit event keyed to the\ncalling admin's real DID. Audit is fail-closed: a change that\ncan't be recorded (no `AuditWriter`) returns 503 rather than\npersisting silently — matching the `/v1/admin/config` doors so\nauditability doesn't depend on which surface the admin used.\nPUT /community/profile — update the community profile. Auth: Admin.\nRefuses changes to the immutable `community_did`.",
        "operationId": "put_profile",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CommunityProfileUpdate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Updated profile + the fields that changed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UpdateProfileResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "Community profile not initialised"
          },
          "503": {
            "description": "Audit writer not configured — change refused"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/community/public-profile": {
      "get": {
        "tags": [
          "community"
        ],
        "summary": "Public GET handler. Trust-Task-exempt, no auth. Returns only the\ncurated subset of profile fields a visitor's browser should see.\nGET /community/public-profile — curated public community profile.\nPublic, unauthenticated.",
        "operationId": "get_public_profile",
        "responses": {
          "200": {
            "description": "Public community profile",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PublicCommunityProfile"
                }
              }
            }
          },
          "404": {
            "description": "Community profile not initialised"
          }
        }
      }
    },
    "/v1/credentials/endorsements": {
      "get": {
        "tags": [
          "endorsements"
        ],
        "operationId": "endorsementList",
        "parameters": [
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": [
                "integer",
                "null"
              ],
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated list of endorsements",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_EndorsementRow"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin or issuer"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "post": {
        "tags": [
          "endorsements"
        ],
        "operationId": "endorsementIssue",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/IssueBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Endorsement issued",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IssueResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin or issuer"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/credentials/endorsements/{id}": {
      "get": {
        "tags": [
          "endorsements"
        ],
        "operationId": "endorsementShow",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Endorsement id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Endorsement",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EndorsementEnvelope"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin or issuer"
          },
          "404": {
            "description": "Endorsement not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "delete": {
        "tags": [
          "endorsements"
        ],
        "operationId": "endorsementRevoke",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Endorsement id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Endorsement revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RevokeResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin or issuer"
          },
          "404": {
            "description": "Endorsement not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/directory/{did}": {
      "get": {
        "tags": [
          "directory"
        ],
        "summary": "`GET /v1/directory/{did}`.",
        "operationId": "query",
        "parameters": [
          {
            "name": "did",
            "in": "path",
            "description": "Subject DID",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fields",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Projected subject record",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DirectoryResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Directory access denied"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/endorsement-types": {
      "get": {
        "tags": [
          "endorsement-types"
        ],
        "operationId": "endorsementTypeList",
        "parameters": [
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": [
                "integer",
                "null"
              ],
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated list of endorsement types",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_EndorsementType"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "post": {
        "tags": [
          "endorsement-types"
        ],
        "operationId": "endorsementTypeRegister",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Endorsement type registered",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/endorsement-types/{type_uri}": {
      "delete": {
        "tags": [
          "endorsement-types"
        ],
        "operationId": "delete",
        "parameters": [
          {
            "name": "type_uri",
            "in": "path",
            "description": "Endorsement type URI",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Endorsement type deleted",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DeleteResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "Endorsement type not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/health/diagnostics": {
      "get": {
        "tags": [
          "health"
        ],
        "operationId": "diagnostics",
        "responses": {
          "200": {
            "description": "Trust-registry reconciler diagnostics",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DiagnosticsResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/install/claim/finish": {
      "post": {
        "tags": [
          "install"
        ],
        "summary": "`POST /v1/install/claim/finish` — complete the WebAuthn install\nceremony, mint the admin DID + setup-session token. Unauthenticated.",
        "operationId": "claim_finish",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ClaimFinishRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Admin DID + setup-session token",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ClaimFinishResponse"
                }
              }
            }
          },
          "401": {
            "description": "Invalid install token or registration state"
          }
        }
      }
    },
    "/v1/install/claim/start": {
      "post": {
        "tags": [
          "install"
        ],
        "summary": "`POST /v1/install/claim/start` — begin the WebAuthn install\nceremony for the first admin. Unauthenticated.",
        "operationId": "claim_start",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ClaimStartRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "WebAuthn creation challenge",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ClaimStartResponse"
                }
              }
            }
          },
          "401": {
            "description": "Invalid install token or claim secret"
          }
        }
      }
    },
    "/v1/invitations": {
      "get": {
        "tags": [
          "invitations"
        ],
        "operationId": "invitationList",
        "responses": {
          "200": {
            "description": "Issued invitations",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InvitationListResponse"
                }
              }
            }
          },
          "403": {
            "description": "Caller is not Admin / Moderator / Issuer"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "post": {
        "tags": [
          "invitations"
        ],
        "operationId": "invitationIssue",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/IssueInvitationBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Invitation issued",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IssueInvitationResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not Admin / Moderator / Issuer"
          },
          "409": {
            "description": "Subject is already a member"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/invitations/{id}": {
      "delete": {
        "tags": [
          "invitations"
        ],
        "operationId": "invitationRevoke",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "VIC id (urn:uuid)",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Invitation revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RevokeResponse"
                }
              }
            }
          },
          "403": {
            "description": "Caller is not Admin / Moderator / Issuer"
          },
          "404": {
            "description": "No such invitation"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/join-requests": {
      "get": {
        "tags": [
          "join-requests"
        ],
        "summary": "GET /join-requests — list join requests (admin work queue). Auth: Admin.",
        "operationId": "list_join_requests",
        "parameters": [
          {
            "name": "status",
            "in": "query",
            "description": "Filter by status. Default `pending` — the operator-facing\nsurface usually wants the work queue.",
            "required": false,
            "schema": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/components/schemas/JoinStatus"
                }
              ]
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": [
                "integer",
                "null"
              ],
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated join requests",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_JoinRequest"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/join-requests/query": {
      "post": {
        "tags": [
          "join-requests"
        ],
        "summary": "`POST /v1/join-requests/query` (admin) — prepare a `credential-exchange/query`\nfor `holderDid` from the Accepts criterion `criterionId`: issue the single-use\nchallenge, **push the query to the holder over DIDComm** when a mediator is\nconfigured, and return the [`QueryBody`] (so a relayer can deliver it when the\npush is unavailable — relayer ≠ holder is supported). The holder presents on\nthe returned `threadId`, and the `credential-exchange/present` handler\nconsumes the challenge.\nPOST /join-requests/query — prepare + push a credential-exchange query to\na holder for a registered Accepts criterion. Auth: Admin.",
        "operationId": "send_query",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SendQueryRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Prepared query + thread to present on",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SendQueryResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "No such Accepts criterion registered"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/join-requests/{id}": {
      "get": {
        "tags": [
          "join-requests"
        ],
        "summary": "GET /join-requests/{id} — show a single join request. Auth: Admin.",
        "operationId": "show_join_request",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Join request id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Join request",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JoinRequestEnvelope"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "Join request not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/join-requests/{id}/decide": {
      "post": {
        "tags": [
          "join-requests"
        ],
        "summary": "POST /join-requests/{id}/decide — decide a pending join request.\n`approved` admits the applicant + issues the VMC; `rejected` refuses\nthem with an optional reason. Auth: Admin.",
        "operationId": "decide",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Join request id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DecideBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Request decided; on approve the VMC + role VEC are returned inline",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DecideResponse"
                }
              }
            }
          },
          "400": {
            "description": "Reject reason exceeds the length cap"
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "Join request not found"
          },
          "409": {
            "description": "Request is not Pending, or applicant is already a member"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/members": {
      "get": {
        "tags": [
          "members"
        ],
        "summary": "GET /members — paginated member list. Auth: Admin.",
        "operationId": "list_members",
        "parameters": [
          {
            "name": "role",
            "in": "query",
            "description": "Filter by role, expressed in the same wire form\n[`VtcRole`] uses (`\"admin\"`, `\"moderator\"`,\n`\"custom:editor\"`, …). Server-side filter applied after\npagination — sibling pages skip rows that don't match.\nFuture improvement: index by role.",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Pagination cursor (returned by a previous call).",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Page size. Clamped to `1..=200`.",
            "required": false,
            "schema": {
              "type": [
                "integer",
                "null"
              ],
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated member list",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_MemberResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/members/me": {
      "delete": {
        "tags": [
          "members"
        ],
        "summary": "DELETE /members/me — self-leave ceremony. Auth: any authenticated member.",
        "operationId": "self_remove",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RemoveBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Member removed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RemoveResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Removal denied by policy"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/members/me/renew": {
      "post": {
        "tags": [
          "members"
        ],
        "summary": "POST /members/me/renew — renew VMC + role VEC. Auth: any authenticated member.",
        "operationId": "renew",
        "responses": {
          "200": {
            "description": "Membership renewed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RenewResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "404": {
            "description": "Caller is not a member"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/members/me/rotate": {
      "post": {
        "tags": [
          "members"
        ],
        "summary": "POST /members/me/rotate — complete a DID rotation. Auth: old DID's session.",
        "operationId": "rotate",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/FinishBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "DID rotated",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FinishResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Session DID does not match oldDid"
          },
          "404": {
            "description": "Caller is not a member"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/members/me/rotate/challenge": {
      "post": {
        "tags": [
          "members"
        ],
        "summary": "POST /members/me/rotate/challenge — mint a DID-rotation challenge.\nAuth: any authenticated member.",
        "operationId": "memberRotateChallenge",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/ChallengeBody"
                  }
                ]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Rotation challenge issued",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChallengeResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "404": {
            "description": "Caller is not a member"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/members/removed": {
      "get": {
        "tags": [
          "members"
        ],
        "summary": "GET /members/removed — members whose row was kept as a tombstone after\ndeparture (no ACL). Auth: Admin. Full scan (departed members are few and\nthis is an operator view), newest-departed first.",
        "operationId": "list_removed",
        "responses": {
          "200": {
            "description": "Departed (tombstoned/historical) members",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RemovedMembersResponse"
                }
              }
            }
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/members/{did}": {
      "get": {
        "tags": [
          "members"
        ],
        "summary": "GET /members/{did} — single member. Auth: Admin.",
        "operationId": "show_member",
        "parameters": [
          {
            "name": "did",
            "in": "path",
            "description": "Member DID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Member record",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MemberEnvelope"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "Member not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "delete": {
        "tags": [
          "members"
        ],
        "summary": "DELETE /members/{did} — admin removes another member. Auth: Admin.",
        "operationId": "admin_remove",
        "parameters": [
          {
            "name": "did",
            "in": "path",
            "description": "Member DID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RemoveBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Member removed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RemoveResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin / removal denied by policy"
          },
          "404": {
            "description": "Member not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "patch": {
        "tags": [
          "members"
        ],
        "summary": "PATCH /members/{did} — update member role + profile fields. Auth: Admin.",
        "operationId": "update_member",
        "parameters": [
          {
            "name": "did",
            "in": "path",
            "description": "Member DID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateMemberRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Updated member record",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MemberEnvelope"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin / role change denied by policy / step-up required for role=admin"
          },
          "404": {
            "description": "Member not found"
          },
          "409": {
            "description": "Target is already an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/members/{did}/personhood": {
      "post": {
        "tags": [
          "members"
        ],
        "summary": "POST /members/{did}/personhood — assert personhood via a VP.\nAuth: any authenticated session.",
        "operationId": "assert",
        "parameters": [
          {
            "name": "did",
            "in": "path",
            "description": "Member DID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AssertBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Personhood asserted",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AssertResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Personhood proof invalid / policy denied"
          },
          "404": {
            "description": "Member not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "delete": {
        "tags": [
          "members"
        ],
        "summary": "DELETE /members/{did}/personhood — revoke personhood. Auth: Admin or self.",
        "operationId": "personhoodRevoke",
        "parameters": [
          {
            "name": "did",
            "in": "path",
            "description": "Member DID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Personhood revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RevokeResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is neither admin nor the subject member"
          },
          "404": {
            "description": "Member not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/members/{did}/personhood/challenge": {
      "post": {
        "tags": [
          "members"
        ],
        "summary": "POST /members/{did}/personhood/challenge — mint a personhood challenge.\nAuth: any authenticated session.",
        "operationId": "personhoodChallenge",
        "parameters": [
          {
            "name": "did",
            "in": "path",
            "description": "Member DID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Personhood challenge minted",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChallengeResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "404": {
            "description": "Member not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/members/{did}/purge": {
      "delete": {
        "tags": [
          "members"
        ],
        "summary": "DELETE /members/{did}/purge — permanently delete a member row, including a\nlingering **tombstone** (a departed member whose row was kept after its ACL\nwas removed). Hard-deletes the ACL (if any) + Member row, decrements the\ncount, and flips the revocation bit. Auth: **Super-admin** (forceful, skips\nthe removal policy). Refuses the sole admin (no-last-admin invariant).",
        "operationId": "purge",
        "parameters": [
          {
            "name": "did",
            "in": "path",
            "description": "Member DID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Member purged",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RemoveResponse"
                }
              }
            }
          },
          "403": {
            "description": "Caller is not a super-admin / would orphan the last admin"
          },
          "404": {
            "description": "No member or tombstone to purge"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/members/{did}/relationships": {
      "get": {
        "tags": [
          "members"
        ],
        "summary": "GET /members/{did}/relationships — paginated VRC list for a member.\nAuth: any authenticated session.",
        "operationId": "memberRelationshipList",
        "parameters": [
          {
            "name": "did",
            "in": "path",
            "description": "Member DID",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": [
                "integer",
                "null"
              ],
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated relationship list",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Paginated_Relationship"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not authorised"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/members/{did}/request-vmc": {
      "post": {
        "tags": [
          "members"
        ],
        "summary": "POST /members/{did}/request-vmc — dispatch a reciprocal-VMC request.",
        "operationId": "request_vmc",
        "parameters": [
          {
            "name": "did",
            "in": "path",
            "description": "Member DID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RequestVmcBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "Request dispatched to the member",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RequestVmcResponse"
                }
              }
            }
          },
          "404": {
            "description": "No active member with that DID"
          },
          "502": {
            "description": "Could not deliver the request to the member"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/policies": {
      "get": {
        "tags": [
          "policies"
        ],
        "operationId": "list_policies",
        "parameters": [
          {
            "name": "purpose",
            "in": "query",
            "description": "Filter by purpose (wire-form camelCase). A VTC extension —\ncanonical has no purpose filter because purpose is not a\nproperty of a module there.",
            "required": false,
            "schema": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/components/schemas/PolicyPurpose"
                }
              ]
            }
          },
          {
            "name": "contextId",
            "in": "query",
            "description": "Canonical `policy/list` parameters this maintainer does not\nimplement. Accepting them silently would tell a caller their\nquery was narrowed when it was not, so they are refused.",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          {
            "name": "enabledOnly",
            "in": "query",
            "required": false,
            "schema": {
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "description": "Canonical page-size name.",
            "required": false,
            "schema": {
              "type": [
                "integer",
                "null"
              ],
              "minimum": 0
            }
          },
          {
            "name": "status",
            "in": "query",
            "description": "`\"active\"` — only the row pointed at by each\n`active_policies:<purpose>`. `\"archived\"` — every row that\nis *not* the current active pointer. Omitted → all rows.",
            "required": false,
            "schema": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/components/schemas/PolicyStatusFilter"
                }
              ]
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Pagination cursor (returned by a previous call).",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Page size. Clamped to `1..=200`.",
            "required": false,
            "schema": {
              "type": [
                "integer",
                "null"
              ],
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated list of policies",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PolicyListResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "post": {
        "tags": [
          "policies"
        ],
        "summary": "Compile + persist a new policy revision. Does NOT activate it —\n`POST /v1/policies/{id}/activate` is a separate call.",
        "operationId": "upload",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UploadBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Policy revision compiled + stored",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UploadResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/policies/active": {
      "get": {
        "tags": [
          "policies"
        ],
        "operationId": "active_policies",
        "parameters": [
          {
            "name": "purpose",
            "in": "query",
            "description": "Narrow to a single decision slot.",
            "required": false,
            "schema": {
              "$ref": "#/components/schemas/PolicyPurpose"
            }
          },
          {
            "name": "contextId",
            "in": "query",
            "description": "Not implemented — a VTC is not context-partitioned. Refused\nrather than ignored, so a caller never reads a community-wide\nbinding as one scoped to their context.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Active policy bindings",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ActiveBindingsResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/policies/{id}": {
      "get": {
        "tags": [
          "policies"
        ],
        "operationId": "show_policy",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Policy id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Policy",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PolicyGetResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "Policy not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/policies/{id}/activate": {
      "post": {
        "tags": [
          "policies"
        ],
        "operationId": "activate",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Policy revision id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Policy revision activated",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ActivateResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "Policy not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/policies/{id}/test": {
      "post": {
        "tags": [
          "policies"
        ],
        "summary": "Evaluate a stored policy against a caller-supplied input.\n**Does not activate** the policy and does not mutate any state\nbeyond log lines. Used by operators to dry-run a candidate\nupload before flipping the active pointer.",
        "operationId": "test",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Policy revision id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TestBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Policy evaluation result",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TestResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "Policy not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/recognition/check": {
      "get": {
        "tags": [
          "recognition"
        ],
        "operationId": "check",
        "parameters": [
          {
            "name": "did",
            "in": "query",
            "description": "The issuer / community DID to test for recognition.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Recognition verdict",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RecognitionCheck"
                }
              }
            }
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/relationships": {
      "post": {
        "tags": [
          "relationships"
        ],
        "operationId": "publish",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PublishBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Relationship (VRC) published",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PublishResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not the VRC issuer or policy denied"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/relationships/graph": {
      "get": {
        "tags": [
          "relationships"
        ],
        "summary": "`GET /v1/relationships/graph` — the community's relationship (VRC) graph for\nthe admin-UI connections view. Admin-gated; a full scan of the relationships\nkeyspace (communities are small and this is operator-only). Edge-derived\nnodes — members with no VRCs don't appear.",
        "description": "Edges are pairs, not individual credentials: each carries the VRCs published\nbetween its two endpoints and a `complete` flag saying whether both\ndirections are present. See the module comment above for why.",
        "operationId": "graph",
        "responses": {
          "200": {
            "description": "Relationship graph",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RelationshipsGraph"
                }
              }
            }
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/relationships/{id}": {
      "delete": {
        "tags": [
          "relationships"
        ],
        "summary": "`DELETE /v1/relationships/{id}` — retract an edge.",
        "description": "## Why there is a body here at all\n\n`revoke` kept the identity equality that `publish` replaced in #1054/#1061:\n`auth.did == rel.issuer_did`. For an edge published under a pairwise\nrelationship DID that compares a membership DID against an R-DID and is\nfalse by construction, so a member could lodge an edge and then never take\nit back — only an admin could. The property the equality was standing in\nfor is *control of the issuing key*, and once the identifier stopped being\nthe member's own, only a proof can establish it.\n\nThree routes to authorization, and the first two are exactly as before:\n\n- **attributed** — `auth.did == rel.issuer_did`. Still correct, still\n  sufficient, no proof needed. The session already demonstrates control of\n  that key.\n- **admin** — moderation, keyed on the row id and not on issuer identity.\n  Unchanged.\n- **pairwise** — a `VrcRevokeAuthorization` signed by the row's\n  `issuerDid`, bound to this row, this community, this session and this\n  moment. New.\n\nLike the publish authorization, **it is verified and discarded** — never\nstored, logged or audited. It carries `sessionId`, which is attributable to\na membership DID, and this handler writes to the audit store, so it is the\none place on the pairwise path where that linkage could plausibly become\ndurable. See `docs/05-design-notes/vrc-publish-proof-of-possession.md`.",
        "operationId": "relationshipRevoke",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Relationship (VRC) id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "description": "Optional. Required only for an edge issued under a pairwise relationship DID.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RevokeBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Relationship (VRC) revoked",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RevokeResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not the issuer or an admin"
          },
          "404": {
            "description": "Relationship not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/relationships/{id}/persona": {
      "post": {
        "tags": [
          "relationships"
        ],
        "summary": "`POST /v1/relationships/{id}/persona` — attach a VPC to a published edge,\nasserting that the party behind the edge's `issuerDid` is this persona.",
        "description": "## How the VPC is bound to the edge — and what is assumed\n\n**This is the part trustoverip/dtgwg-cred-spec#9 has not settled.** A VPC\nnames its persona (`issuer`) and the counterparty (`credentialSubject.id`).\nIt does *not* name the relationship DID the persona used, so a VPC on its\nown does not identify an edge, and nothing in the current specification\nsays how it should.\n\nRather than invent a credential-level binding and present it as settled,\nthe binding here is made at the *request* level, out of three parts:\n\n1. the caller names the edge, by id, in the URL;\n2. the caller proves control of that edge's `issuerDid` — the same\n   proof-of-possession construction publishing the edge required\n   (`docs/05-design-notes/vrc-publish-proof-of-possession.md`);\n3. the VPC's `credentialSubject.id` must equal the edge's `subjectDid`.\n\n(2) is what makes it safe: the only party who could have published this\nedge is the only party who can annotate it, so attaching a persona is\nexactly as authorized as publishing the edge was. (3) is a consistency\ncheck, not a binding — it rules out attaching a persona asserted to some\n*other* counterparty. Nothing is added to the VPC and no claim is made\nabout how the specification should resolve #9; if it lands an in-credential\nbinding (a `digest` over the VRC, as the VWC has), this endpoint can\nrequire that as well without changing the stored shape.\n\n**Known limitation of (3).** DTG Credentials says the VPC's subject is\n\"typically the R-DID or M-DID used in the relationship\". A VPC whose\nsubject is the counterparty's *M-DID*, on an edge whose `subjectDid` is\ntheir *R-DID*, is rejected here. That case is real and this endpoint\ncannot presently accept it — resolving it needs the same #9 answer.\n\n## The persona is the edge issuer's\n\nOne VRC is one direction of an edge, and the persona it carries belongs to\nthe party who issued it. The counterparty asserts their own persona on\ntheir own reciprocal VRC. This keeps each half-edge self-contained and\nmeans neither party can put words in the other's mouth.",
        "operationId": "attach_persona",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Relationship (VRC) id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AttachPersonaBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Persona (VPC) attached",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PersonaResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller does not control the edge's issuer"
          },
          "404": {
            "description": "Relationship not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "delete": {
        "tags": [
          "relationships"
        ],
        "summary": "`DELETE /v1/relationships/{id}/persona` — withdraw the persona from an\nedge, leaving the edge itself in place.",
        "description": "This exists because the assertion it reverses is a disclosure. A member who\ncan correlate their edges under a persona but can never stop is worse off\nthan one who never could, so the withdrawal has to be as available as the\nassertion — and gated the same way, or anyone could strip another member's\npersona.\n\nIdempotent: detaching from an edge that carries no persona is a 200 with a\nnull `personaDid`, matching `delete_relationship`'s convention. No audit\nentry is written in that case — nothing changed.\n\nThe authorization object is carried in the request body. `DELETE` with a\nbody is unusual, but the alternative is putting a signed object in a query\nstring, and the proof of control has to travel with the request somehow.",
        "operationId": "detach_persona",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Relationship (VRC) id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DetachPersonaBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Persona (VPC) detached",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PersonaResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller does not control the edge's issuer"
          },
          "404": {
            "description": "Relationship not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/relationships/{id}/restore": {
      "post": {
        "tags": [
          "relationships"
        ],
        "summary": "`POST /v1/relationships/{id}/restore` — reverse a suspension.",
        "description": "Reverses a suspension and nothing else. An edge that has expired, been\nsuperseded or been withdrawn is refused with a 409, and the boundary is\ndeliberate — see the module doc of [`crate::credentials::lifecycle`] on\nrestoration versus replacement. Restoring an edge whose `validUntil` passed\nwhile it was suspended *succeeds* (the suspension is genuinely reversed)\nand the response reports `expired`, because a recorded event cannot extend\na window the issuer signed.",
        "operationId": "restore",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Relationship (VRC) id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "description": "Optional. `pop` is required only for an edge issued under a pairwise relationship DID.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LifecycleBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Suspension reversed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LifecycleResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not the issuer or an admin"
          },
          "404": {
            "description": "Relationship not found"
          },
          "409": {
            "description": "Edge is not suspended"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/relationships/{id}/suspend": {
      "post": {
        "tags": [
          "relationships"
        ],
        "summary": "`POST /v1/relationships/{id}/suspend` — make an edge temporarily\nineffective without withdrawing it.",
        "description": "Authorized exactly as revocation is (issuer session DID, admin, or a\n`VrcSuspendAuthorization` proving control of a pairwise issuer), because it\nis the same question about the same edge. It is not, however, the same\n*act*: revocation deletes the row and is unrecoverable, while this appends\nan event and leaves a supported way back.\n\nRefused with a 409 if the edge is already suspended, or if it has been\nsuperseded or withdrawn. Those are conflicts rather than validation errors\n— the request is well-formed and the caller is entitled to make it; it is\nthe edge's state that refuses, and for a suspension a retry after a\nrestoration would succeed.",
        "operationId": "suspend",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Relationship (VRC) id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "description": "Optional. `pop` is required only for an edge issued under a pairwise relationship DID.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LifecycleBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Relationship suspended",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LifecycleResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not the issuer or an admin"
          },
          "404": {
            "description": "Relationship not found"
          },
          "409": {
            "description": "Edge is already suspended, superseded or withdrawn"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/schemas": {
      "get": {
        "tags": [
          "schemas"
        ],
        "summary": "`GET /v1/schemas` — list every registered per-type schema.",
        "operationId": "schemaList",
        "responses": {
          "200": {
            "description": "List of registered schemas",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/SchemaEntry"
                  }
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "post": {
        "tags": [
          "schemas"
        ],
        "summary": "`POST /v1/schemas` — register (or update) a per-type schema entry.",
        "operationId": "schemaRegister",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterSchemaBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Schema registered",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SchemaEntry"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/schemas/accepts": {
      "get": {
        "tags": [
          "schemas"
        ],
        "summary": "`GET /v1/schemas/accepts` — list every Accepts criterion.",
        "operationId": "list_accepts_route",
        "responses": {
          "200": {
            "description": "List of Accepts criteria",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/AcceptsCriterion"
                  }
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "post": {
        "tags": [
          "schemas"
        ],
        "summary": "`POST /v1/schemas/accepts` — register (or update) an Accepts criterion. The\nDCQL query is validated and every referenced type checked against the\nregistry by [`store_accepts`].",
        "operationId": "register_accepts",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterAcceptsBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Accepts criterion registered",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AcceptsCriterion"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/schemas/accepts/{id}": {
      "get": {
        "tags": [
          "schemas"
        ],
        "summary": "`GET /v1/schemas/accepts/{id}` — fetch one Accepts criterion.",
        "operationId": "get_accepts_route",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Accepts criterion id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Accepts criterion",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AcceptsCriterion"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "Accepts criterion not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "delete": {
        "tags": [
          "schemas"
        ],
        "summary": "`DELETE /v1/schemas/accepts/{id}` — remove an Accepts criterion.",
        "operationId": "delete_accepts_route",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Accepts criterion id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Accepts criterion deleted",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DeleteResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "Accepts criterion not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/schemas/{type_uri}": {
      "get": {
        "tags": [
          "schemas"
        ],
        "summary": "`GET /v1/schemas/{type_uri}` — fetch one registered schema.",
        "operationId": "get_one",
        "parameters": [
          {
            "name": "type_uri",
            "in": "path",
            "description": "Credential type URI",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Schema entry",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SchemaEntry"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "Schema not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      },
      "delete": {
        "tags": [
          "schemas"
        ],
        "summary": "`DELETE /v1/schemas/{type_uri}` — remove a registered schema.",
        "operationId": "delete_one",
        "parameters": [
          {
            "name": "type_uri",
            "in": "path",
            "description": "Credential type URI",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Schema deleted",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DeleteResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid bearer token"
          },
          "403": {
            "description": "Caller is not an admin"
          },
          "404": {
            "description": "Schema not found"
          }
        },
        "security": [
          {
            "bearer_jwt": []
          }
        ]
      }
    },
    "/v1/status-lists/{purpose}": {
      "get": {
        "tags": [
          "status-lists"
        ],
        "summary": "GET /status-lists/{purpose} — public BitstringStatusList credential.\nUnauthenticated, Trust-Task-exempt verifier endpoint.",
        "operationId": "statusListShow",
        "parameters": [
          {
            "name": "purpose",
            "in": "path",
            "description": "'revocation' or 'suspension'",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Bitstring status list credential",
            "content": {
              "application/json": {}
            }
          },
          "404": {
            "description": "Unknown or unprovisioned purpose"
          },
          "503": {
            "description": "Credential signer or state not ready"
          }
        }
      }
    },
    "/v1/trust-tasks": {
      "post": {
        "tags": [
          "trust-tasks"
        ],
        "summary": "POST /trust-tasks — dispatch a Trust Task document. Public: the holder's\ndocument proof (or, over DIDComm, the authcrypt sender) IS the auth.",
        "operationId": "dispatch",
        "requestBody": {
          "description": "A Trust Task document (trust_tasks_rs::TrustTask JSON)",
          "content": {
            "text/plain": {
              "schema": {
                "type": "string"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Trust Task #response document"
          },
          "400": {
            "description": "Malformed document / payload (trust-task-error)"
          },
          "403": {
            "description": "Holder auth / VIC verification failed (trust-task-error)"
          },
          "422": {
            "description": "Task failed, e.g. duplicate request (trust-task-error)"
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "AcceptsCriterion": {
        "type": "object",
        "description": "A named required-evidence criterion: a DCQL query over the schema-store\nregistry that a ceremony runs to decide whether a holder's presented\ncredentials satisfy the community's acceptance rules.",
        "required": [
          "id",
          "query",
          "createdAt",
          "createdByDid"
        ],
        "properties": {
          "createdAt": {
            "type": "string",
            "format": "date-time"
          },
          "createdByDid": {
            "type": "string",
            "description": "Admin DID that registered the criterion (audit correlation)."
          },
          "description": {
            "type": [
              "string",
              "null"
            ],
            "description": "Free-form description shown in admin UIs."
          },
          "id": {
            "type": "string",
            "description": "Criterion id (e.g. a ceremony purpose or a named manifest). Primary key."
          },
          "query": {
            "description": "The DCQL query, stored as JSON. Structurally validated and every\nreferenced type checked against the registry when stored (see\n[`validate_accepts_query`])."
          }
        }
      },
      "AclEntryEnvelope": {
        "type": "object",
        "description": "`{ entry: … }` — the shape `acl/{grant,show,change-role}/0.1` publish.\n\nAll three returned the row bare until #1109. The row itself always\nconformed; only the wrapper was missing, which is the same envelope defect\nthe VTC family carried on eight tasks. It went unseen here for longer\nbecause the shared `spec/{acl,audit,auth,config,policy}/*` families are\noutside the conformance table's census — a stated limit, since both daemons\nserve them — so no fixture ever described these responses. The\nresponse-conformance layer needed no census: it validated what the handler\nsent.",
        "required": [
          "entry"
        ],
        "properties": {
          "entry": {
            "$ref": "#/components/schemas/AclEntryResponse"
          }
        }
      },
      "AclEntryResponse": {
        "type": "object",
        "description": "Canonical `acl/_shared` **AclEntry**.\n\nRenames from VTC's storage shape: `did` → `subject`,\n`allowed_contexts` → `scopes`. Timestamps are RFC3339 strings, not\nunix epochs — canonical types them `format: date-time`, and an\ninteger there would be a silent contract break rather than a\ncosmetic one.",
        "required": [
          "subject",
          "role",
          "scopes",
          "createdAt",
          "createdBy"
        ],
        "properties": {
          "createdAt": {
            "type": "string"
          },
          "createdBy": {
            "type": "string"
          },
          "expiresAt": {
            "type": [
              "string",
              "null"
            ]
          },
          "label": {
            "type": [
              "string",
              "null"
            ]
          },
          "role": {
            "$ref": "#/components/schemas/VtcRole"
          },
          "scopes": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "subject": {
            "type": "string"
          },
          "updatedAt": {
            "type": [
              "string",
              "null"
            ]
          },
          "updatedBy": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "AclListResponse": {
        "type": "object",
        "required": [
          "entries",
          "truncated"
        ],
        "properties": {
          "cursor": {
            "type": [
              "string",
              "null"
            ]
          },
          "entries": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AclEntryResponse"
            }
          },
          "truncated": {
            "type": "boolean",
            "description": "True when more entries match beyond this page; `cursor` is then\npresent. Required by canonical `acl/list`."
          }
        }
      },
      "ActivateResponse": {
        "type": "object",
        "required": [
          "activated",
          "purpose"
        ],
        "properties": {
          "activated": {
            "type": "string",
            "format": "uuid",
            "description": "Canonical name for the id of the policy now in force."
          },
          "ext": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Value",
                "description": "The activated module's source hash, under `ext`.\n\nIt was a top-level `sha256` until #1110 — and its own doc called it a\n\"VTC extension\", which is exactly what the framework's `ext` slot is\nfor. The response is `additionalProperties: false`, so an extension\ncarried beside the canonical members is not an extension, it is a\nviolation; `ext` is the sanctioned place and costs one level of\nnesting."
              }
            ]
          },
          "previousPolicyId": {
            "type": [
              "string",
              "null"
            ],
            "format": "uuid",
            "description": "Predecessor active policy id for this purpose. `null` for\nthe first activation under a given purpose."
          },
          "purpose": {
            "$ref": "#/components/schemas/PolicyPurpose"
          }
        }
      },
      "ActiveBinding": {
        "type": "object",
        "description": "One activation binding. `contextId` is omitted throughout: a VTC is\na single community and does not partition its policy set per trust\ncontext, so every binding is community-wide.",
        "required": [
          "purpose",
          "policy"
        ],
        "properties": {
          "policy": {
            "$ref": "#/components/schemas/PolicyModuleResponse"
          },
          "purpose": {
            "type": "string"
          }
        }
      },
      "ActiveBindingsResponse": {
        "type": "object",
        "description": "Canonical `policy/active` response: the `(contextId, purpose) →\nmodule` bindings currently in force.",
        "required": [
          "bindings"
        ],
        "properties": {
          "bindings": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ActiveBinding"
            }
          }
        }
      },
      "AdminSessionRequest": {
        "type": "object",
        "description": "Request body for [`admin_session`].",
        "required": [
          "accessToken"
        ],
        "properties": {
          "accessToken": {
            "type": "string",
            "description": "A valid VTC access token the caller already holds — e.g. from the\nVTA-wallet SIOP login, which returns it in `tokens.accessToken`."
          }
        }
      },
      "AdminSessionResponse": {
        "type": "object",
        "description": "`POST /v1/auth/admin-session` — exchange a bearer access token for the\nadmin SPA's cookie session.\n\nThe VTA-wallet login path returns a bearer token in the response body\n(the wallet extension posts the SIOP `id_token` to `/wallet/auth/` and\nreads `tokens.accessToken`), but the admin SPA drives the API with the\n`vtc_admin_session` cookie + `csrf` double-submit, not an\n`Authorization` header. This endpoint bridges the two: it validates the\npresented token (signature + VTC audience + expiry) and, on success,\nsets the `vtc_admin_session` + `csrf` cookie pair:\n\n- `vtc_admin_session=<jwt>; Path=/; SameSite=Strict; Secure; HttpOnly` —\n  the access-token JWT, scoped to the daemon's whole origin so the browser\n  sends it on `/v1/*` API calls. `HttpOnly` keeps JS from reading it;\n  `SameSite=Strict` prevents cross-site CSRF.\n- `csrf=<random>; Path=/; SameSite=Strict; Secure` (HttpOnly **false**, so\n  SPA JS can mirror the value into the `X-CSRF-Token` header for the\n  double-submit check in `routing::csrf`).\n\nNo privilege escalation — the caller must already possess a valid VTC\naccess token, which it could use directly as a bearer; this only mirrors\nit into the cookie the browser SPA expects. Browser-only by nature: the\nCSRF layer's same-origin check carries the (cookie-less) first call.\n`{ sessionId, expiresAt }` — the shape `vtc/auth/admin-session/0.1`\npublishes, and which this route did not return until #1059's witness put\nthe handler beside its own schema.",
        "required": [
          "sessionId",
          "expiresAt"
        ],
        "properties": {
          "expiresAt": {
            "type": "integer",
            "format": "int64",
            "description": "Unix seconds, from the token's own expiry — the same value the\ncookies' `Max-Age` is derived from, so the two cannot disagree.",
            "minimum": 0
          },
          "sessionId": {
            "type": "string"
          }
        }
      },
      "AssertBody": {
        "type": "object",
        "required": [
          "presentation"
        ],
        "properties": {
          "presentation": {
            "$ref": "#/components/schemas/Value",
            "description": "W3C Verifiable Presentation. `holder` must equal the\npath-DID; `proof.challenge` must equal a fresh challenge\nid from `POST .../personhood/challenge`."
          }
        }
      },
      "AssertResponse": {
        "type": "object",
        "required": [
          "did",
          "personhood",
          "vmc",
          "roleVec"
        ],
        "properties": {
          "did": {
            "type": "string"
          },
          "personhood": {
            "type": "boolean"
          },
          "roleVec": {
            "$ref": "#/components/schemas/Value"
          },
          "vmc": {
            "$ref": "#/components/schemas/Value"
          }
        }
      },
      "AttachPersonaBody": {
        "type": "object",
        "required": [
          "vpc"
        ],
        "properties": {
          "pop": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Value",
                "description": "Proof that the caller controls the key behind the edge's\n`issuerDid`, when that is not the caller's session DID.\nRequired for every pairwise edge, since a relationship\nDID is never the session DID by construction."
              }
            ]
          },
          "vpc": {
            "$ref": "#/components/schemas/Value",
            "description": "The self-issued VPC, in the DTG Credentials wire form:\n`type` including `PersonaCredential`, `issuer` the P-DID\nof the persona, `credentialSubject.id` the counterparty's\nDID, and a data-integrity proof. Built by\n`dtg_credentials::DTGCredential::new_vpc`."
          }
        }
      },
      "AuditEntry": {
        "type": "object",
        "description": "One entry in the canonical `audit/list` response.\n\n`additionalProperties: false` on the canonical envelope means VTC's\nextra fields cannot ride along at the top level, so the keyed hashes\nand the per-variant version move into `ext`.",
        "required": [
          "eventId",
          "recordedAt",
          "action",
          "schemaVersion",
          "prevHash",
          "entryHash",
          "detail",
          "ext"
        ],
        "properties": {
          "action": {
            "type": "string"
          },
          "actor": {
            "type": [
              "string",
              "null"
            ]
          },
          "detail": {},
          "entryHash": {
            "type": "string"
          },
          "eventId": {
            "type": "string"
          },
          "ext": {},
          "prevHash": {
            "type": "string",
            "description": "Multibase-wrapped multihash, which is what the canonical schema's\ndigest pattern accepts: `^(z[1-9A-HJ-NP-Za-km-z]+|u[A-Za-z0-9_-]+)$`.\n\nThese were bare hex until #1110, chosen so a caller need not reconcile\nagainst the base64 the *stored* envelope uses. That reasoning was about\ntwo internal encodings and overlooked the third — the one the published\nschema requires. A bare digest is also self-describing in neither its\nhash function nor its encoding, which is the problem multihash and\nmultibase each exist to solve.\n\nThe chain is untouched: the stored envelope still holds `[u8; 32]` and\nhashes over bytes. This is the wire view only."
          },
          "recordedAt": {
            "type": "string"
          },
          "schemaVersion": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "target": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "AuditListResponse": {
        "type": "object",
        "description": "Canonical `audit/list` response.",
        "required": [
          "entries",
          "truncated"
        ],
        "properties": {
          "cursor": {
            "type": [
              "string",
              "null"
            ]
          },
          "entries": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AuditEntry"
            }
          },
          "truncated": {
            "type": "boolean",
            "description": "True when more entries match beyond this page; `cursor` is then\npresent."
          }
        }
      },
      "AuthenticateResponse": {
        "type": "object",
        "description": "Server responds from `POST /auth/`. Conforms to\n`spec/auth/authenticate/0.1#response`: `{ session, tokens }`.",
        "required": [
          "session",
          "tokens"
        ],
        "properties": {
          "session": {
            "$ref": "#/components/schemas/Session"
          },
          "tokens": {
            "$ref": "#/components/schemas/TokenBundle"
          }
        }
      },
      "BootstrapRequest": {
        "type": "object",
        "required": [
          "setupSessionToken"
        ],
        "properties": {
          "setupSessionToken": {
            "type": "string"
          }
        }
      },
      "BootstrapResponse": {
        "type": "object",
        "required": [
          "adminDid",
          "eventId"
        ],
        "properties": {
          "adminDid": {
            "type": "string"
          },
          "eventId": {
            "type": "string",
            "format": "uuid",
            "description": "`event_id` of the persisted `CommunityInstalled` audit\nenvelope. The caller can echo this in operator-facing UI so\nthe install URL → bootstrap → audit-row chain is traceable."
          }
        }
      },
      "CeremonyListResponse": {
        "type": "object",
        "description": "`{ ceremonies: […] }` — the shape `vtc/ceremonies/list/0.1` publishes. The\nhandler sent a top-level array until #1094. An array cannot grow a sibling\nmember later without breaking every reader; the envelope is why the spec\nasks for one.",
        "required": [
          "ceremonies"
        ],
        "properties": {
          "ceremonies": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CeremonyManifest"
            }
          }
        }
      },
      "CeremonyManifest": {
        "type": "object",
        "required": [
          "purpose",
          "pkg",
          "nature",
          "label",
          "wired",
          "blurb",
          "fields",
          "factsTemplate"
        ],
        "properties": {
          "blurb": {
            "type": "string"
          },
          "factsTemplate": {
            "$ref": "#/components/schemas/Value",
            "description": "JSON skeleton of the verified-Facts `input`, with `$field:<key>`\n/ `$now` / `$if` directives the admin-UI materializes."
          },
          "fields": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FieldDef"
            }
          },
          "label": {
            "type": "string"
          },
          "nature": {
            "type": "string"
          },
          "pkg": {
            "type": "string"
          },
          "purpose": {
            "type": "string"
          },
          "wired": {
            "type": "string"
          }
        }
      },
      "ChainBreakReport": {
        "type": "object",
        "description": "A detected break, flattened for the wire.\n\n`ChainBreak` itself is deliberately not `Serialize` in\n`vti-common`, so this is the REST projection of it.",
        "required": [
          "kind",
          "index",
          "eventId"
        ],
        "properties": {
          "eventId": {
            "type": "string",
            "description": "`event_id` of the offending envelope."
          },
          "index": {
            "type": "integer",
            "description": "Position in the ascending walk, counting skipped rows.",
            "minimum": 0
          },
          "kind": {
            "type": "string",
            "description": "`tamperedEntry` — the envelope's content was altered after it\nwas written; or `brokenLink` — an entry was reordered,\ndropped, or inserted."
          }
        }
      },
      "ChallengeBody": {
        "type": "object",
        "description": "Optional body for the challenge request.\n\nAbsent body, absent field, and `unspecified` all mean the same\nthing — the member declined to say. The endpoint predates this\nfield, so clients that send no body at all stay valid.",
        "properties": {
          "reason": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/DidRotationReason",
                "description": "Why the member is rotating. Self-asserted — see\n[`DidRotatedData::rotation_reason`] for what this is and is not\nevidence of."
              }
            ]
          }
        }
      },
      "ChallengeRequest": {
        "type": "object",
        "description": "Client sends to `POST /auth/challenge`.\n\nWire shape conforms to `spec/auth/challenge/0.1`: the `did` field\nserialises as `subject` per the canonical payload schema. The Rust\nidentifier stays `did` for consistency with `AuthClaims.did` and\nthe rest of the codebase. `alias = \"did\"` keeps clients that still\nsend the legacy name working through one upgrade cycle.",
        "required": [
          "subject"
        ],
        "properties": {
          "subject": {
            "type": "string"
          }
        }
      },
      "ChallengeResponse": {
        "type": "object",
        "required": [
          "challengeId",
          "expiresAt",
          "ext"
        ],
        "properties": {
          "challengeId": {
            "type": "string",
            "format": "uuid"
          },
          "expiresAt": {
            "type": "string",
            "format": "date-time"
          },
          "ext": {
            "$ref": "#/components/schemas/Value",
            "description": "Vendor-namespaced extension members (SPEC §4.5.1). Carries\n[`match_code::MATCH_CODE_EXT_KEY`] — the eight characters the\nadmin and the member read to each other to confirm they are\nlooking at the same ceremony. See [`crate::members::match_code`]\nfor why the code rides here rather than as a top-level field."
          }
        }
      },
      "ClaimFinishRequest": {
        "type": "object",
        "required": [
          "installToken",
          "registrationId",
          "webauthnResponse"
        ],
        "properties": {
          "installToken": {
            "type": "string"
          },
          "registrationId": {
            "type": "string"
          },
          "webauthnResponse": {
            "type": "object"
          }
        }
      },
      "ClaimFinishResponse": {
        "type": "object",
        "required": [
          "adminDid",
          "setupSessionToken"
        ],
        "properties": {
          "adminDid": {
            "type": "string"
          },
          "setupSessionToken": {
            "type": "string"
          }
        }
      },
      "ClaimStartRequest": {
        "type": "object",
        "required": [
          "installToken"
        ],
        "properties": {
          "claimSecret": {
            "type": [
              "string",
              "null"
            ],
            "description": "Out-of-band claim code the operator received alongside the\ninstall URL. Required when the persisted token row has a\n`claim_secret_hash`; ignored otherwise so legacy tokens\n(and tests) keep working. The route handler verifies the\ncode against the hash before issuing the WebAuthn challenge\n— a wrong or missing code returns 401 with discriminated\nerror codes (`claim_secret_required` / `claim_secret_invalid`)."
          },
          "installToken": {
            "type": "string"
          }
        }
      },
      "ClaimStartResponse": {
        "type": "object",
        "required": [
          "registrationId",
          "options"
        ],
        "properties": {
          "options": {
            "type": "object",
            "description": "The WebAuthn `PublicKeyCredentialCreationOptions` payload —\nthe operator's UA passes this to `navigator.credentials.create()`."
          },
          "registrationId": {
            "type": "string",
            "description": "Echoes the install token's `jti`. Consumer must pass this\nback to `claim/finish` so the server can index the stored\nregistration state."
          }
        }
      },
      "CommunityProfile": {
        "type": "object",
        "description": "The singleton record. Field names are wire contract — operators\n+ the admin UX read this shape directly.",
        "required": [
          "communityDid",
          "name",
          "description",
          "language",
          "createdAt"
        ],
        "properties": {
          "communityDid": {
            "type": "string",
            "description": "Immutable — set at install time. PUT requests cannot change\nthis; see [`CommunityProfileUpdate`]."
          },
          "contactEmail": {
            "type": [
              "string",
              "null"
            ]
          },
          "createdAt": {
            "type": "string",
            "format": "date-time"
          },
          "description": {
            "type": "string"
          },
          "extensions": {
            "description": "Opaque per-community JSON. Capped at [`MAX_EXTENSIONS_BYTES`]\nwhen serialised. Defaults to `null` when no extension data\nis set.\n\nOmitted from the wire when null. The canonical `CommunityProfile`\ncomponent types this `object` and does not require it, so absent is the\nconforming way to say \"none\" and `null` is a type error. It went out as\n`null` until #1107 — the conformance fixture set a non-empty object, so\na hand-chosen value hid it even after the fixture was built from this\nvery struct. Deriving a fixture from the real type only helps for the\nmembers you do not then hand-set."
          },
          "language": {
            "type": "string",
            "description": "BCP 47 language tag. Defaults to `\"en\"`."
          },
          "logoUrl": {
            "type": [
              "string",
              "null"
            ]
          },
          "name": {
            "type": "string"
          },
          "personhood": {
            "$ref": "#/components/schemas/PersonhoodGovernance",
            "description": "What this community's governance says about personhood.\n\nDTG Credentials §Personhood Credentials puts PHC status here rather\nthan in the credential: \"PHC status is determined by governance and\ntrust registries, not by credential structure\", and the\n`PersonhoodCredential` type this daemon stamps on a vetted member's\nVMC is described there as a *non-authoritative hint*. §Governance\nConsiderations item 2 is blunter still: \"Whether a VMC qualifies as a\nPHC is a governance determination, not a schema property.\"\n\nWithout this field a conformant verifier had nothing to read. The\ntype array told it not to trust the type array, and there was no\nsecond place to look."
          },
          "publicUrl": {
            "type": [
              "string",
              "null"
            ]
          },
          "relationshipIdentifierDefault": {
            "type": "string",
            "description": "Which identifier form this community expects members to issue\nrelationship credentials under — `\"attributed\"` (the member's\nmembership DID, so edges name them) or `\"pairwise\"` (a relationship\nDID unique to each counterparty).\n\nA **declaration, not an enforcement**: the member still chooses per\nrelationship, and both forms are accepted. It exists so a client can\nread what the community expects *before* minting — a public\nopen-source community reasonably declares `attributed`, one organised\naround privacy declares `pairwise`.\n\nA community that wants to *require* a form does so in\n`relationships.rego`, which receives `identifier_form` on every\npublish. Defaults to `\"pairwise\"`, matching DTG Credentials'\nrecommendation, so a community that has not considered the question\ndeclares the more conservative expectation.\n\n`serde(default)` is load-bearing: a config export taken before this\nfield existed must still import. Without it every saved export becomes\nunreadable — which the admin-config round-trip tests caught."
          }
        }
      },
      "CommunityProfileResponse": {
        "type": "object",
        "description": "GET response shape.\n\nWraps the persisted [`CommunityProfile`] + adds the live\n`registryStatus` field surfaced by Phase 3 M3.2.\n`registry_status` is read from `AppState` at request time,\nnot persisted on the profile row.",
        "required": [
          "profile"
        ],
        "properties": {
          "profile": {
            "$ref": "#/components/schemas/ProfileWithStatus",
            "description": "Nested, because `vtc/community/profile/show/0.1` requires a\n`profile` member and is `additionalProperties: false`. This was\n`#[serde(flatten)]` until #1094, which put every profile field at the\ntop level — where the schema permits none of them."
          }
        }
      },
      "CommunityProfileUpdate": {
        "type": "object",
        "description": "PUT-shaped patch. Distinct from [`CommunityProfile`] because the\n`community_did` and `created_at` fields are immutable — exposing\nthem on the request body invites tampering, so we drop them at\nthe type level.\n\nEvery field is `Option` so a PUT can update a subset of fields\nwhile leaving the rest unchanged. Setting `extensions: Some(Value::Null)`\nclears the blob; omitting it (`None`) leaves it untouched.",
        "properties": {
          "contactEmail": {
            "type": [
              "string",
              "null"
            ]
          },
          "description": {
            "type": [
              "string",
              "null"
            ]
          },
          "extensions": {},
          "language": {
            "type": [
              "string",
              "null"
            ]
          },
          "logoUrl": {
            "type": [
              "string",
              "null"
            ]
          },
          "name": {
            "type": [
              "string",
              "null"
            ]
          },
          "personhood": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/PersonhoodGovernance",
                "description": "See [`PersonhoodGovernance`]. Replaced wholesale rather than merged\nfield-by-field: the two booleans are a conjunction the operator is\nasserting together, and a partial patch that flipped one while leaving\nthe other at whatever it happened to be is how a community ends up\nclaiming PHC status it did not mean to claim."
              }
            ]
          },
          "publicUrl": {
            "type": [
              "string",
              "null"
            ]
          },
          "relationshipIdentifierDefault": {
            "type": [
              "string",
              "null"
            ],
            "description": "See [`CommunityProfile::relationship_identifier_default`]. Must be one\nof [`RELATIONSHIP_IDENTIFIER_FORMS`]."
          }
        }
      },
      "ConfigExportDocument": {
        "type": "object",
        "description": "The portable configuration document — canonical\n`vtc/_shared/0.1/config-portability#ConfigExportDocument`.\n\n`community_profile` is `None` when the community hasn't been\ninitialised yet (pre-bootstrap); the member is omitted rather\nthan emitted as `null`, because an import cannot distinguish a\nsynthesised empty profile from a real blank one.\n`config_overrides` carries *only* the db-layer keys — env-layer\nand toml-layer values stay per-host and aren't portable.\n\n`schema_version` is not redundant with the `0.1` in the Trust\nTask URI. The URI versions the *envelope*; the moment an operator\nwrites this document to a file the envelope is gone and it is\nbare JSON, so `schemaVersion` is the only thing a later reader has\nto check it against.",
        "required": [
          "schemaVersion",
          "exportedAt",
          "configOverrides"
        ],
        "properties": {
          "communityProfile": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/CommunityProfile"
              }
            ]
          },
          "configOverrides": {
            "type": "object",
            "additionalProperties": {},
            "propertyNames": {
              "type": "string"
            }
          },
          "exportedAt": {
            "type": "string",
            "format": "date-time"
          },
          "schemaVersion": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "ConfigSource": {
        "type": "string",
        "description": "Where the **prior** value came from in the four-layer overlay.\nMirrors the variant on `vti_common::audit::ConfigSource` (M0.1.5).",
        "enum": [
          "env",
          "db",
          "toml",
          "default"
        ]
      },
      "CreateAclRequest": {
        "type": "object",
        "description": "Canonical `acl/grant` request: the entry the maintainer should hold\nfor the subject, plus an optional operator rationale.",
        "required": [
          "entry"
        ],
        "properties": {
          "entry": {
            "$ref": "#/components/schemas/GrantEntry"
          },
          "reason": {
            "type": [
              "string",
              "null"
            ],
            "description": "Operator rationale. Emitted on the service log line for this\nchange; the audit envelope's data types do not carry a free-text\nreason today, so this is deliberately not described as audited."
          }
        },
        "additionalProperties": false
      },
      "CreateInviteRequest": {
        "type": "object",
        "required": [
          "did"
        ],
        "properties": {
          "did": {
            "type": "string",
            "description": "Admin DID the install URL grants a passkey for."
          },
          "label": {
            "type": [
              "string",
              "null"
            ],
            "description": "Operator-supplied label for the ACL entry if one needs to be\ncreated. Ignored when the ACL entry already exists."
          },
          "ttlSeconds": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Token TTL in seconds. Defaults to\n[`INSTALL_TOKEN_DEFAULT_TTL_SECS`] (15 minutes) when omitted.\nCap at 24 h to keep stale invites from accumulating in the\nkeyspace.",
            "minimum": 0
          }
        }
      },
      "CreateInviteResponse": {
        "type": "object",
        "required": [
          "jti",
          "installUrl",
          "claimCode",
          "expiresAt",
          "aclEntryCreated"
        ],
        "properties": {
          "aclEntryCreated": {
            "type": "boolean",
            "description": "`true` when the call also wrote the ACL entry; `false` when\nthe target DID was already an admin (idempotent)."
          },
          "claimCode": {
            "type": "string",
            "description": "Out-of-band claim code the invitee must type alongside the\nURL to complete the ceremony. Returned **once** — the\ndaemon stores only its Argon2id hash, so a lost code\nrequires re-minting the invite. The operator delivers URL\nand code through separate channels."
          },
          "expiresAt": {
            "type": "string",
            "format": "date-time",
            "description": "Wall-clock expiry for the token."
          },
          "installUrl": {
            "type": "string",
            "description": "Clickable install URL pointing at the admin SPA's install\npage on this daemon's `public_url`."
          },
          "jti": {
            "type": "string",
            "description": "`jti` of the minted install token — also the key the GET +\nDELETE surfaces use."
          }
        }
      },
      "CredentialReference": {
        "type": "object",
        "description": "A pointer to the issued VEC: its identifier and lifetime, not its bytes.\n\n#1098 mapped this to the registry-wide `IssuedCredential`, whose\n`credential` and `expiresAt` are required and neither of which is on the\nstored row — so `list` and `show` could not fill it and stayed\nnon-conformant. That turned out to be the component being used in the\nwrong place rather than a gap in this service: `IssuedCredential` is\nscoped to the moment of minting, and a listing is not an issuance event.\ntrustoverip/dtgwg-trust-tasks-tf#262 splits the two, and this is the read\nside of that split. The credential itself now rides on the `issue`\nresponse alone, which is the one call whose caller cannot get it any\nother way.",
        "required": [
          "credentialId"
        ],
        "properties": {
          "credentialId": {
            "type": "string"
          },
          "expiresAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time"
          },
          "issuedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time"
          }
        }
      },
      "DecideBody": {
        "type": "object",
        "required": [
          "decision"
        ],
        "properties": {
          "decision": {
            "$ref": "#/components/schemas/Decision"
          },
          "reason": {
            "type": [
              "string",
              "null"
            ],
            "description": "Optional operator rationale, recorded in the audit trail.\nChiefly useful with `rejected`."
          }
        }
      },
      "DecideResponse": {
        "type": "object",
        "required": [
          "requestId",
          "status"
        ],
        "properties": {
          "requestId": {
            "type": "string",
            "format": "uuid"
          },
          "roleVec": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Value",
                "description": "Issued role VEC. Same delivery story as `vmc`."
              }
            ]
          },
          "status": {
            "type": "string"
          },
          "vmc": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Value",
                "description": "Issued VMC (M2.12). Also pushed to the applicant's wallet over\nDIDComm on approve (best-effort); kept inline so the admin caller\ncan still hand it over out-of-band if that delivery doesn't land.\n`None` on the reject path."
              }
            ]
          }
        }
      },
      "Decision": {
        "type": "string",
        "description": "The two ways a pending request can be decided\n(`vtc/join-requests/decide/0.1`'s `decision` enum).",
        "enum": [
          "approved",
          "rejected"
        ]
      },
      "DeleteResponse": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "id": {
            "type": "string"
          }
        }
      },
      "DetachPersonaBody": {
        "type": "object",
        "properties": {
          "pop": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Value",
                "description": "As for [`AttachPersonaBody::pop`], with\n`type: \"VpcDetachAuthorization\"` and no `vpc` field."
              }
            ]
          }
        }
      },
      "DiagnosticsResponse": {
        "type": "object",
        "description": "Phase 3 M3.8 — admin-gated diagnostics view.\n\nSurfaces enough of the trust-registry reconciler's internal\nstate to debug \"is sync stuck?\" without shelling onto the\nhost. The fields are picked for *operator action*, not raw\nmetrics dump:\n\n- `registry_status` — does the daemon think the registry\n  is reachable right now? Mirrors the same flag on\n  `GET /v1/community/profile` (M3.2).\n- `queue_depth` — count of pending+InFlight rows. A\n  monotonically rising number means the registry is\n  refusing dispatch.\n- `oldest_pending_age_seconds` — staleness of the\n  longest-waiting job. Drives the \"≥1h behind →\n  degraded\" SLI per spec §8.3.\n- `rtbf_batched_count` — how many jobs are parked behind\n  the RTBF window. Lets operators verify the batch\n  protection is active without inspecting fjall.\n- `failed_count` — terminal-failure rows the syncer gave\n  up on (`attempts > max_attempts` or\n  `RegistryError::Permanent`). These need operator\n  triage; the syncer won't retry them.\n- `last_success_at` / `last_failure_at` / `last_error` —\n  replayed verbatim from the health probe state.\n\nDiagnostics is **admin-gated**, not super-admin: ops staff\nrunning on-call should be able to read this without\nholding the super-admin role.",
        "required": [
          "registryStatus",
          "queueDepth",
          "rtbfBatchedCount",
          "failedCount",
          "syncerEnabled",
          "syncerRunning",
          "syncerRestarts",
          "messagingStatus",
          "transports"
        ],
        "properties": {
          "failedCount": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "lastError": {
            "type": [
              "string",
              "null"
            ]
          },
          "lastFailureAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time"
          },
          "lastSuccessAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time"
          },
          "mediatorDid": {
            "type": [
              "string",
              "null"
            ],
            "description": "The mediator's DID. Also surfaced (post-bootstrap) on the\nunauth `/v1/community/public-profile`."
          },
          "mediatorUrl": {
            "type": [
              "string",
              "null"
            ],
            "description": "The mediator's HTTPS endpoint. Infrastructure topology —\nadmin-gated so it's not a free unauth recon oracle."
          },
          "messagingStatus": {
            "type": "string",
            "description": "Live messaging connectivity (D2 P1a): `\"connected\"` when the delivery\nlayer's transport reports a live mediator websocket, else\n`\"disconnected\"` — including before the listener has bound. Read off\n[`MessagingService::status`](affinidi_messaging_delivery::MessagingService::status),\nwhich tracks the transport's re-falsifiable connection signal (never a\nboot-time latch, per R6.2). `\"disconnected\"` when messaging is\nunconfigured."
          },
          "oldestPendingAgeSeconds": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "queueDepth": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "registryStatus": {
            "type": "string"
          },
          "registryTransport": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/RegistryTransport",
                "description": "How this VTC reaches its **trust registry**: its DID, the protocols the\nregistry's own document advertises, and the one the last call actually\nchose. `None` when no registry is configured.\n\nAdvertised and active are reported separately on purpose. A registry\nthat advertises TSP while this VTC can only answer DIDComm is\n*configured* and *unreachable* at the same time, and one collapsed\n\"protocol\" field cannot say so — it would have to pick one of the two\nfacts and drop the other."
              }
            ]
          },
          "rtbfBatchedCount": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "syncerEnabled": {
            "type": "boolean",
            "description": "Whether the `MembershipSyncer` task is enabled (a registry is\nconfigured) and currently running (P3.13). `syncer_enabled &&\n!syncer_running` means the syncer is spawned but dead — e.g.\nmid-restart after a panic. `syncer_restarts` counts panic\nrestarts; a rising value is a \"syncer keeps crashing\" signal."
          },
          "syncerRestarts": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "syncerRunning": {
            "type": "boolean"
          },
          "transports": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TransportStatus"
            },
            "description": "This VTC's **own** transports: per protocol, whether its DID document\nadvertises it and whether this build can serve it right now.\n\nThe same view the unauthenticated community profile publishes, from the\nsame helper — an operator comparing the two should never see them\ndisagree. Empty when the VTC's DID could not be resolved, which is\n\"unknown\", not \"none advertised\"."
          },
          "vtaDid": {
            "type": [
              "string",
              "null"
            ],
            "description": "DID of the VTA the VTC was provisioned against. Moved here\nfrom the unauth `/health` payload (P3.7)."
          }
        }
      },
      "DidRotationReason": {
        "type": "string",
        "description": "Why a member rotated their DID, as declared at challenge time.\n\nSeparate from [`crate::audit::RotationReason`], which describes\n*audit-HMAC-key* rotation: that enum serializes PascalCase, is\nalready persisted in `audit_key:` rows (so it cannot be re-cased),\nand its variants (`Initial`, `Routine` meaning \"the background task\nfired\") carry no sensible reading in this domain.",
        "enum": [
          "routine",
          "compromise",
          "deviceLoss",
          "migration",
          "unspecified"
        ]
      },
      "DirectoryResponse": {
        "type": "object",
        "description": "The projected subject record.",
        "required": [
          "subject",
          "fields"
        ],
        "properties": {
          "fields": {
            "type": "object",
            "additionalProperties": {
              "$ref": "#/components/schemas/Value"
            },
            "propertyNames": {
              "type": "string"
            }
          },
          "subject": {
            "type": "string"
          }
        }
      },
      "Disposition": {
        "type": "string",
        "description": "Spec §5.5 disposition for a removal. Determines what happens to\nthe Member record + status-list slot on member departure.",
        "enum": [
          "purge",
          "tombstone",
          "historical",
          "policydefault"
        ]
      },
      "EffectiveConfig": {
        "type": "object",
        "description": "Response shape for `GET /v1/admin/config` — every registry key\nresolved through the four-layer overlay.",
        "required": [
          "fields"
        ],
        "properties": {
          "fields": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/EffectiveField"
            }
          }
        }
      },
      "EffectiveField": {
        "type": "object",
        "description": "One field in the merged [`EffectiveConfig`] view. Per spec §14.6\nthe admin UX needs both the value and the *source* layer so an\noperator can tell whether the running value was set in TOML, by\nan env override, by a PATCH against the DB layer, or fell back\nto the compiled-in default.",
        "required": [
          "key",
          "value",
          "source",
          "requiresRestart"
        ],
        "properties": {
          "key": {
            "type": "string"
          },
          "requiresRestart": {
            "type": "boolean"
          },
          "source": {
            "$ref": "#/components/schemas/ConfigSource"
          },
          "value": {}
        }
      },
      "EndorsementEnvelope": {
        "type": "object",
        "description": "`{ endorsement: … }` — the shape `vtc/endorsements/show/0.1` publishes.",
        "required": [
          "endorsement"
        ],
        "properties": {
          "endorsement": {
            "$ref": "#/components/schemas/EndorsementRow"
          }
        }
      },
      "EndorsementRow": {
        "type": "object",
        "description": "One endorsement as the canonical `Endorsement` component names it.\n\nA wire type distinct from the stored [`Endorsement`] because renaming the\nstored fields would rewrite the persisted fjall rows — `Endorsement`\nderives `Deserialize` and serialises `camelCase`, so `id` /\n`endorsementType` / `createdAt` are the on-disk keys of every row already\nwritten. Mapping at the boundary keeps the wire correct without a data\nmigration, the same split `GenerationRow` uses (#1095).",
        "required": [
          "endorsementId",
          "typeUri",
          "subjectDid",
          "issued",
          "statusListIndex",
          "claim"
        ],
        "properties": {
          "claim": {
            "$ref": "#/components/schemas/Value"
          },
          "endorsementId": {
            "type": "string",
            "format": "uuid",
            "description": "`endorsementId`, stored as `id`."
          },
          "issued": {
            "$ref": "#/components/schemas/CredentialReference",
            "description": "A reference to the issued credential — **not** a timestamp.\n\n#1096 read `issued` as \"when it was issued\" and mapped `createdAt`\nonto it, so this went out as an RFC 3339 string where the component\nrequires an object."
          },
          "revokedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time"
          },
          "statusListIndex": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "subjectDid": {
            "type": "string"
          },
          "typeUri": {
            "type": "string",
            "description": "`typeUri`, stored as `endorsementType`."
          }
        }
      },
      "EndorsementType": {
        "type": "object",
        "description": "A registered endorsement type. Stored verbatim; the\nregistrar route enforces validation at insert time.",
        "required": [
          "typeUri",
          "createdAt",
          "createdByDid"
        ],
        "properties": {
          "claimSchema": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Value",
                "description": "Optional JSON Schema for the claim body. Reserved for\nfuture per-type validation; the Phase 4 issuance path\nonly checks \"type is registered\" without consulting\nthe schema. Operators can read the schema from\n`GET /v1/endorsement-types/{uri}` and validate\nclient-side."
              }
            ]
          },
          "createdAt": {
            "type": "string",
            "format": "date-time"
          },
          "createdByDid": {
            "type": "string",
            "description": "Admin DID that registered the type. Carried for\naudit correlation against the\n`EndorsementTypeRegistered` envelope."
          },
          "description": {
            "type": [
              "string",
              "null"
            ],
            "description": "Free-form description shown in admin UIs."
          },
          "typeUri": {
            "type": "string",
            "description": "The type URI. Primary key — URL-encoded into the\nkeyspace key."
          }
        }
      },
      "ExportResponse": {
        "type": "object",
        "description": "`POST /v1/admin/config/export` response — canonical\n`vtc/config/export/0.1#response`. The document is returned under a\nnamed member rather than as the bare body: the registry response\nconvention requires `additionalProperties: false` plus an `ext`\nextension point, and neither attaches to a bare `$ref`.",
        "required": [
          "document"
        ],
        "properties": {
          "document": {
            "$ref": "#/components/schemas/ConfigExportDocument"
          }
        }
      },
      "FieldDef": {
        "type": "object",
        "required": [
          "key",
          "label",
          "type",
          "default"
        ],
        "properties": {
          "default": {
            "$ref": "#/components/schemas/Value"
          },
          "hint": {
            "type": [
              "string",
              "null"
            ]
          },
          "key": {
            "type": "string"
          },
          "label": {
            "type": "string"
          },
          "options": {
            "type": [
              "array",
              "null"
            ],
            "items": {
              "$ref": "#/components/schemas/FieldOption"
            }
          },
          "showWhen": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ShowWhen"
              }
            ]
          },
          "type": {
            "type": "string"
          }
        }
      },
      "FieldDiff": {
        "type": "object",
        "description": "A single field an import would change, or did — canonical\n`vtc/_shared/0.1/config-portability#ConfigFieldChange`.\n\n`oldValue` is omitted when the key isn't currently set (no profile\nyet, or no db-layer override). `newValue` is omitted when the\ndocument leaves the field out, which means \"leave the live value\nalone\" — distinct from an explicit `null`, which means \"clear it\".\nBoth are serialised as *absent* rather than `null` so that\ndistinction survives on the wire.",
        "required": [
          "key"
        ],
        "properties": {
          "key": {
            "type": "string"
          },
          "newValue": {},
          "oldValue": {}
        }
      },
      "FieldOption": {
        "type": "object",
        "required": [
          "value",
          "label"
        ],
        "properties": {
          "label": {
            "type": "string"
          },
          "value": {
            "type": "string"
          }
        }
      },
      "FinishBody": {
        "type": "object",
        "required": [
          "rotationId",
          "oldDid",
          "newDid",
          "oldSignature",
          "newSignature"
        ],
        "properties": {
          "newDid": {
            "type": "string"
          },
          "newSignature": {
            "type": "string",
            "description": "Hex-encoded Ed25519 signature by the new DID's key."
          },
          "oldDid": {
            "type": "string"
          },
          "oldSignature": {
            "type": "string",
            "description": "Hex-encoded Ed25519 signature by the old DID's key."
          },
          "rotationId": {
            "type": "string",
            "format": "uuid"
          }
        }
      },
      "FinishResponse": {
        "type": "object",
        "required": [
          "newDid",
          "method",
          "vmc",
          "roleVec"
        ],
        "properties": {
          "method": {
            "type": "string"
          },
          "newDid": {
            "type": "string"
          },
          "roleVec": {
            "$ref": "#/components/schemas/Value"
          },
          "vmc": {
            "$ref": "#/components/schemas/Value"
          }
        }
      },
      "GrantEntry": {
        "type": "object",
        "description": "The writable subset of a canonical `AclEntry`. Server-owned fields\n(`createdAt`/`createdBy`/`updatedAt`/`updatedBy`) are deliberately\nabsent — a caller must not be able to backdate provenance.",
        "required": [
          "subject",
          "role"
        ],
        "properties": {
          "expiresAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "RFC3339, per canonical `AclEntry.expiresAt`."
          },
          "label": {
            "type": [
              "string",
              "null"
            ]
          },
          "role": {
            "$ref": "#/components/schemas/VtcRole"
          },
          "scopes": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "subject": {
            "type": "string"
          }
        },
        "additionalProperties": false
      },
      "GraphEdge": {
        "type": "object",
        "description": "One edge between a pair of identifiers, carrying every VRC published between\nthem.",
        "required": [
          "endpoints",
          "halves",
          "complete"
        ],
        "properties": {
          "complete": {
            "type": "boolean",
            "description": "An **in-force** VRC exists in both directions — a complete DTG edge,\nand the only form in which both parties currently consent.\n\n\"In force\" and not merely \"present\" since #1079. A half whose VRC has\nexpired, or which has been suspended, superseded or withdrawn, no\nlonger completes an edge — resolved through\n[`Relationship::in_force_at`], which is the one place the precedence\nrule lives. Before this the graph was read back without re-checking\nanything, so a half-edge whose reciprocal had expired years ago was\nindistinguishable from a live mutual relationship.\n\nThe halves themselves are still listed whatever their state: an\noperator looking at a graph needs to see that a VRC was published, and\nremoving it would make a withdrawn edge look like one that never\nexisted. Which halves are in force is not yet surfaced per half — that\nneeds a field on `GraphHalf`, and `relationships/graph/0.2` pins the\nresponse shape with `additionalProperties: false`, so it waits on a\nspec revision upstream rather than shipping a body the task rejects."
          },
          "endpoints": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The two endpoints, DID-sorted. Always exactly two entries, and sorting\nthem gives the pair one identity whichever half was published first.\nBoth entries are equal only for a self-issued VRC."
          },
          "halves": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/GraphHalf"
            },
            "description": "Every VRC published between the endpoints, oldest first. One for a\nhalf-edge, two for the ordinary complete edge; more only if a party\npublished several VRCs in the same direction, which nothing prevents\n(idempotency is per credential hash, not per direction)."
          }
        }
      },
      "GraphHalf": {
        "type": "object",
        "description": "One published VRC: a directed *half* of a DTG edge, from `issuerDid` (the\nasserting party) to `subjectDid`. Body-free (no VRC JSON-LD) — the graph\nshows the shape, not the credential contents. `id` is the row id, which is\nwhat `DELETE /v1/relationships/{id}` takes.",
        "required": [
          "id",
          "issuerDid",
          "subjectDid",
          "createdAt"
        ],
        "properties": {
          "createdAt": {
            "type": "string"
          },
          "id": {
            "type": "string"
          },
          "issuerDid": {
            "type": "string"
          },
          "personaDid": {
            "type": [
              "string",
              "null"
            ],
            "description": "The persona (P-DID) the issuer has asserted on this edge, if any.\n\nThis is the one place the deliberate correlation a VPC exists to\nenable becomes visible: two pairwise edges carrying the same\n`personaDid` are the same party, said so by that party. Without it the\nannotation would be stored and never read, which is the state #1067\ndescribes."
          },
          "subjectDid": {
            "type": "string"
          }
        }
      },
      "GraphNode": {
        "type": "object",
        "description": "One node in the connections graph — an identifier that is an endpoint of at\nleast one relationship (VRC). Isolated members (no VRCs) are not surfaced.",
        "required": [
          "did"
        ],
        "properties": {
          "did": {
            "type": "string"
          }
        }
      },
      "HealthStatus": {
        "type": "string",
        "description": "Wire-form reachability state. Mirrors what `GET\n/v1/community/profile` surfaces in `registryStatus`.",
        "enum": [
          "active",
          "degraded"
        ]
      },
      "ImportRequest": {
        "type": "object",
        "description": "`POST /v1/admin/config/import` request — canonical\n`vtc/config/import/0.1`.\n\n`confirm` rides in the **payload**, not a query string: a Trust\nTask is the same interface over REST, DIDComm and TSP, and only\none of those three has a query string to put it in.\n\nIt defaults to `false` because the safe direction of a default is\nthe one whose mistake is recoverable — a caller who meant to apply\nand previewed loses a round-trip, where the reverse has already\noverwritten a live community's configuration.",
        "required": [
          "document"
        ],
        "properties": {
          "confirm": {
            "type": "boolean"
          },
          "document": {
            "$ref": "#/components/schemas/ConfigExportDocument"
          }
        },
        "additionalProperties": false
      },
      "ImportResponse": {
        "type": "object",
        "description": "`POST /v1/admin/config/import` response — canonical\n`vtc/config/import/0.1#response`.\n\nOne shape for both paths. On a preview the change arrays are what\n*would* be written; on an apply they are what *was*. That is why\nthere are no separate `*Applied` lists: a caller reads `status` to\nknow which it is holding.",
        "required": [
          "status",
          "profileChanges",
          "overrideChanges",
          "pendingRestart",
          "rejected"
        ],
        "properties": {
          "overrideChanges": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FieldDiff"
            },
            "description": "Configuration-override keys that differ from what is in force.\nRejected keys are not listed here — they appear under\n`rejected`."
          },
          "pendingRestart": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Applied keys whose new value takes effect only after a\nrestart. Reported on a preview too, so an operator learns that\nthe import implies downtime *before* confirming it."
          },
          "profileChanges": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FieldDiff"
            },
            "description": "Community-profile members that differ from what is in force.\nEmpty when the document omits `communityProfile`, or when\nnothing differs."
          },
          "rejected": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RejectedKey"
            },
            "description": "Keys rejected by validation (unknown registry key, type\nmismatch, value-out-of-range, oversized extensions blob).\nPopulated identically on preview and apply, so a preview\nsurfaces every rejection before anything is written."
          },
          "status": {
            "$ref": "#/components/schemas/ImportStatus",
            "description": "`preview` when `confirm` was not set; `imported` after the\ndocument was applied."
          }
        }
      },
      "ImportStatus": {
        "type": "string",
        "description": "Whether an import response describes a dry run or a completed apply.",
        "enum": [
          "preview",
          "imported"
        ]
      },
      "InForce": {
        "oneOf": [
          {
            "type": "object",
            "description": "Inside its window, with no standing event against it.",
            "required": [
              "state"
            ],
            "properties": {
              "state": {
                "type": "string",
                "enum": [
                  "yes"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Its `validFrom` has not arrived.",
            "required": [
              "validFrom",
              "state"
            ],
            "properties": {
              "state": {
                "type": "string",
                "enum": [
                  "notYetValid"
                ]
              },
              "validFrom": {
                "type": "string",
                "format": "date-time"
              }
            }
          },
          {
            "type": "object",
            "description": "Its `validUntil` has passed. Reported even when a `Restored` event\nstands, because restoration returns an artifact to its window rather\nthan extending it.",
            "required": [
              "validUntil",
              "state"
            ],
            "properties": {
              "state": {
                "type": "string",
                "enum": [
                  "expired"
                ]
              },
              "validUntil": {
                "type": "string",
                "format": "date-time"
              }
            }
          },
          {
            "type": "object",
            "description": "Temporarily ineffective. Reversible, and the only non-`Yes` state that\nis.",
            "required": [
              "since",
              "state"
            ],
            "properties": {
              "since": {
                "type": "string",
                "format": "date-time"
              },
              "state": {
                "type": "string",
                "enum": [
                  "suspended"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Displaced by a later artifact, named by digest.",
            "required": [
              "at",
              "by",
              "state"
            ],
            "properties": {
              "at": {
                "type": "string",
                "format": "date-time"
              },
              "by": {
                "type": "string"
              },
              "state": {
                "type": "string",
                "enum": [
                  "superseded"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Withdrawn. Terminal.",
            "required": [
              "at",
              "state"
            ],
            "properties": {
              "at": {
                "type": "string",
                "format": "date-time"
              },
              "state": {
                "type": "string",
                "enum": [
                  "withdrawn"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "The artifact's own window could not be read, so no honest answer is\navailable.\n\nReachable on rows written before #1075, which entered the graph without\ntheir window ever being parsed. Surfaced rather than defaulted, for the\nreason `credentials::ingress::rejects_an_unparseable_bound` pins: the\nfailure mode that makes a temporal guard useless is treating an\nunreadable bound as \"no bound stated\". [`Self::is_in_force`] is false\nhere, so the safe reading is the automatic one.",
            "required": [
              "reason",
              "state"
            ],
            "properties": {
              "reason": {
                "type": "string"
              },
              "state": {
                "type": "string",
                "enum": [
                  "indeterminate"
                ]
              }
            }
          }
        ],
        "description": "Whether an artifact is currently in force, and if not, why not.\n\nA single enum rather than a `bool` plus a reason string, for the reason\n`credentials::witness::WitnessBinding` gives for the same choice: the\nvariants distinguish situations a caller genuinely needs to tell apart, and\ncollapsing them loses the one distinction that matters most — between an\nartifact whose own issuer bounded it and one this community stopped."
      },
      "InvitationListItem": {
        "type": "object",
        "description": "One row of the invitation list (the registry record, body-free).",
        "required": [
          "id",
          "subjectDid",
          "issuedBy",
          "issuedAt"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "issuedAt": {
            "type": "string"
          },
          "issuedBy": {
            "type": "string"
          },
          "revokedAt": {
            "type": [
              "string",
              "null"
            ]
          },
          "role": {
            "type": [
              "string",
              "null"
            ]
          },
          "subjectDid": {
            "type": "string"
          },
          "validUntil": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "InvitationListResponse": {
        "type": "object",
        "required": [
          "invitations"
        ],
        "properties": {
          "invitations": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/InvitationListItem"
            }
          }
        }
      },
      "InviteStatus": {
        "type": "string",
        "enum": [
          "issued",
          "consumed",
          "expired"
        ]
      },
      "InviteSummary": {
        "type": "object",
        "required": [
          "jti",
          "status"
        ],
        "properties": {
          "consumedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "`Consumed` rows record the wall-clock at which the\nceremony succeeded."
          },
          "expiresAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "`Issued` token expiry. `None` for `Consumed` rows (the\nstate machine clears the timing data on transition)."
          },
          "jti": {
            "type": "string"
          },
          "status": {
            "$ref": "#/components/schemas/InviteStatus"
          },
          "targetDid": {
            "type": [
              "string",
              "null"
            ],
            "description": "Admin DID the invite was minted for. `None` only on\nlegacy rows persisted before the field landed (those are\nsafe to clean up via revoke). New invites always carry it."
          }
        }
      },
      "IssueBody": {
        "type": "object",
        "required": [
          "subjectDid",
          "typeUri",
          "claim"
        ],
        "properties": {
          "claim": {
            "$ref": "#/components/schemas/Value"
          },
          "subjectDid": {
            "type": "string"
          },
          "typeUri": {
            "type": "string",
            "description": "`typeUri`, the name the payload schema gives it. This carried an\nexplicit `#[serde(rename = \"type\")]` until #1096 — a deliberate\nrename that simply disagreed with the spec."
          },
          "validitySeconds": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Optional override; defaults to 30d.",
            "minimum": 0
          }
        }
      },
      "IssueInvitationBody": {
        "type": "object",
        "required": [
          "subjectDid"
        ],
        "properties": {
          "role": {
            "type": [
              "string",
              "null"
            ],
            "description": "Optional role to grant the invitee on join (e.g. `member`, `moderator`,\n`issuer`). Carried in the VIC's `credentialSubject.scopes` as\n`role:<name>` and honored by the join policy. `admin` is refused — the\nno-admin-via-join privilege ceiling would deny it anyway. Defaults to\n`member` (absent scope)."
          },
          "subjectDid": {
            "type": "string",
            "description": "The DID to invite (a prospective, non-member holder)."
          },
          "validityDays": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "description": "Optional validity in days (1..=90); defaults to the 7-day VIC default.",
            "minimum": 0
          }
        }
      },
      "IssueInvitationResponse": {
        "type": "object",
        "required": [
          "subjectDid",
          "vic"
        ],
        "properties": {
          "subjectDid": {
            "type": "string",
            "description": "Echo of the invited DID."
          },
          "validUntil": {
            "type": [
              "string",
              "null"
            ],
            "description": "The VIC's `validUntil` (RFC3339), for the operator UI to display."
          },
          "vic": {
            "$ref": "#/components/schemas/Value",
            "description": "The signed Invitation Credential — handed to the invitee out-of-band\n(copy / QR). The invitee presents it back in a join request."
          }
        }
      },
      "IssueResponse": {
        "type": "object",
        "required": [
          "endorsement",
          "credential"
        ],
        "properties": {
          "credential": {
            "$ref": "#/components/schemas/Value",
            "description": "The signed credential just minted, which only this call can hand\nback. A read carries `endorsement.issued` — the reference — instead,\nso a page of fifty endorsements does not embed fifty credentials."
          },
          "endorsement": {
            "$ref": "#/components/schemas/EndorsementRow"
          }
        }
      },
      "JoinDecision": {
        "type": "object",
        "description": "The refusal, in the form the applicant is owed it.\n\nBoth rejection paths reach the applicant through the same poll, but\nthe evidence used to sit in two different places and only one of them\nwas projected: an auto-deny left a serialized `Deny` verdict on\n[`JoinRequest::policy_decision`], and an admin reject wrote the\noperator's reason to the audit log and nowhere else. So an\nadmin-rejected applicant could never learn why — the correlated\nceremony reply is a one-shot delivery, and the poll that exists to\nrecover a missed one returned bare `{requestId, status}`.\n\nOne field written by both paths is the fix, and it is deliberately\n*not* `policy_decision`: an operator's decision is not a policy\nverdict, and recording it as one would make the audit trail lie about\nwhere the refusal came from.",
        "required": [
          "code",
          "decidedAt"
        ],
        "properties": {
          "code": {
            "type": "string",
            "description": "Stable refusal code. From the policy's `deny` verdict on the\nauto-deny path; [`vta_sdk::protocols::join_requests::ADMIN_REJECT_CODE`]\non the admin path, where there is no verdict to source one from."
          },
          "decidedAt": {
            "type": "string",
            "format": "date-time",
            "description": "When the decision was taken — not when the poll answering it was\nproduced. For an admin reject the two diverge by however long the\napplicant takes to poll."
          },
          "reason": {
            "type": [
              "string",
              "null"
            ],
            "description": "Optional elaboration — the policy's `reason`, or the operator's\nwords. Absent when the decider supplied none."
          }
        }
      },
      "JoinRequest": {
        "type": "object",
        "description": "One join request. Stored under `join_requests:<id>`.",
        "required": [
          "id",
          "applicantDid",
          "vp",
          "submittedAt",
          "status"
        ],
        "properties": {
          "applicantDid": {
            "type": "string"
          },
          "decision": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/JoinDecision",
                "description": "Why this request was refused, for the applicant.\n\nWritten by **both** rejection paths — the policy auto-deny at\nsubmit and the admin reject — so the status poll has one field\nto read rather than two shapes to reconcile. [`JoinDecision`]\nsays why that matters.\n\n`None` for a request that has not been rejected, and for one\nrejected before this field existed (see\n[`JoinRequest::decision_for_applicant`], which reconstructs what\nit can from [`Self::policy_decision`])."
              }
            ]
          },
          "extensions": {
            "$ref": "#/components/schemas/Value",
            "description": "Community-defined extensions slot (spec §3-M). Bounded by\n`JOIN_REQUEST_EXTENSIONS_MAX_BYTES` (16 KiB) at the route\nlayer.\nOmitted when null. The canonical `JoinRequest` component types this\n`object` and does not require it, so absent is how \"none\" is spelled\nand `null` is a type error — the same shape as `policyDecision` in\n#1099. Found by the response-conformance layer on real traffic; the\nfixture set a non-empty object and so never saw it."
          },
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "policyDecision": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Value",
                "description": "Set by Phase 2's policy step on approve / reject. Always\n`None` in Phase 1.\n\nOmitted rather than sent as `null`: the canonical `JoinRequest`\ncomponent types this `object`, not `[\"object\", \"null\"]` as it does\nthe neighbouring `vpClaims` and `decision`. It is not required, so\nabsent is the conforming way to say \"no policy decision\" — `null` is\na type error. It went out as `null` until #1099."
              }
            ]
          },
          "registryConsent": {
            "type": "boolean",
            "description": "Whether the applicant consents to being published in the\ncommunity's trust-registry record (spec §8). Default\n`false`; the operator-facing surface defers this decision\nto Phase 3."
          },
          "status": {
            "$ref": "#/components/schemas/JoinStatus"
          },
          "submittedAt": {
            "type": "string",
            "format": "date-time"
          },
          "vp": {
            "$ref": "#/components/schemas/Value",
            "description": "Opaque VP carried verbatim from the wire. Phase 1 never\ninspects it beyond the holder-binding check the route layer\nran at submit time; Phase 2's policy step reads from\n[`Self::vp_claims`] (the canonical projection extracted at\nsubmit time), not from this field."
          },
          "vpClaims": {
            "$ref": "#/components/schemas/Value",
            "description": "Canonical projection of [`Self::vp`] computed at submit\ntime by [`crate::policy::extract::extract_vp_claims`] and\nfed to `join.rego` as `input.vp_claims`. Stored on the row\nso the approve flow doesn't have to re-extract (plan §D4).\n`null` on rows persisted before Phase 2."
          }
        }
      },
      "JoinRequestEnvelope": {
        "type": "object",
        "description": "`{ request: … }` — the shape `vtc/join-requests/show/0.1` publishes.",
        "required": [
          "request"
        ],
        "properties": {
          "request": {
            "$ref": "#/components/schemas/JoinRequest"
          }
        }
      },
      "JoinStatus": {
        "type": "string",
        "description": "State of a join request through its lifecycle.",
        "enum": [
          "pending",
          "approved",
          "rejected",
          "withdrawn",
          "deferred"
        ]
      },
      "LifecycleBody": {
        "type": "object",
        "properties": {
          "pop": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Value",
                "description": "Proof that the caller controls the key behind the row's `issuerDid`,\nwhen that is not the caller's session DID — i.e. for every edge\npublished under a pairwise relationship DID. Verified by the same\n`authorize_edge_control` gate revocation uses, with a `type` distinct\nto this verb."
              }
            ]
          },
          "reason": {
            "type": [
              "string",
              "null"
            ],
            "description": "Optional operator- or member-supplied note, stored verbatim on the\nevent.\n\nRecorded because a suspension a reader cannot interpret is close to\nuseless: \"temporarily ineffective, cause unstated\" gives the\ncounterparty nothing to act on. It is deliberately free text and\ndeliberately optional — the state machine never reads it."
          }
        }
      },
      "LifecycleEvent": {
        "allOf": [
          {
            "$ref": "#/components/schemas/LifecycleEventKind"
          },
          {
            "type": "object",
            "required": [
              "recordedAt"
            ],
            "properties": {
              "recordedAt": {
                "type": "string",
                "format": "date-time",
                "description": "Transaction time: when this service recorded the event, **not** when\nthe underlying decision was taken. [`resolve`] ignores events recorded\nafter the instant being asked about, which is what makes \"was this in\nforce at T\" answerable at all."
              }
            }
          }
        ],
        "description": "One entry in an artifact's lifecycle log: what happened, and when this\nservice recorded it."
      },
      "LifecycleEventKind": {
        "oneOf": [
          {
            "type": "object",
            "description": "Temporarily ineffective, not withdrawn. The distinction matters to the\nparty being suspended as much as to a verifier: a suspension says the\nrelying party should stop relying *for now*, and leaves a supported\nroute back.",
            "required": [
              "event"
            ],
            "properties": {
              "event": {
                "type": "string",
                "enum": [
                  "suspended"
                ]
              },
              "reason": {
                "type": [
                  "string",
                  "null"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Reverses a suspension, returning the artifact to the governance of its\nown window. Never valid against any other standing state — see the\nmodule doc on restoration versus replacement.",
            "required": [
              "event"
            ],
            "properties": {
              "event": {
                "type": "string",
                "enum": [
                  "restored"
                ]
              },
              "reason": {
                "type": [
                  "string",
                  "null"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A later artifact has displaced this one. Terminal.\n\n`by` names the displacing artifact by digest rather than by row id, so\nthe record stays meaningful to a reader who holds the credential but\nnot this service's storage — the same reason `vrcDigestMultibase` is\nthe identifier the publish response returns.",
            "required": [
              "by",
              "event"
            ],
            "properties": {
              "by": {
                "type": "string"
              },
              "event": {
                "type": "string",
                "enum": [
                  "superseded"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Withdrawn by the party who controls it, or by the community. Terminal.\n\nDistinct from deleting the row: a withdrawal that is *recorded* can be\nresolved against a past instant, where a deletion can only be observed\nas an absence.",
            "required": [
              "event"
            ],
            "properties": {
              "event": {
                "type": "string",
                "enum": [
                  "withdrawn"
                ]
              },
              "reason": {
                "type": [
                  "string",
                  "null"
                ]
              }
            }
          }
        ],
        "description": "A lifecycle event recorded against an artifact after it was issued.\n\nThe variants are the vocabulary #1079 says is missing. They are separate\nvariants rather than a `status: String` because the transition rules in\n[`LifecycleLog::record`] have to distinguish them, and a stringly-typed\nstate machine is one typo away from accepting a transition it rejects."
      },
      "LifecycleLog": {
        "type": "array",
        "items": {
          "$ref": "#/components/schemas/LifecycleEvent"
        },
        "description": "The append-only lifecycle log of one artifact, oldest first.\n\nAppend-only and monotonic in `recorded_at`, both enforced by\n[`Self::record`]. Order in the vector *is* the precedence order, so an\nout-of-order insert would silently change the answer for every instant\nafter it."
      },
      "LifecycleResponse": {
        "type": "object",
        "required": [
          "id",
          "state"
        ],
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "state": {
            "$ref": "#/components/schemas/InForce",
            "description": "The edge's resolved state after the event, as\n[`crate::credentials::lifecycle::resolve`] computes it.\n\nThe *resolved* state, not the event that was just recorded, because\nthose are not the same thing and the difference is the point of the\nprecedence rule: restoring an edge whose `validUntil` has passed\nrecords a restoration and still answers `expired`."
          }
        }
      },
      "ListInvitesResponse": {
        "type": "object",
        "required": [
          "invites"
        ],
        "properties": {
          "invites": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/InviteSummary"
            }
          }
        }
      },
      "ListResponse": {
        "type": "object",
        "required": [
          "credentials"
        ],
        "properties": {
          "credentials": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RegisteredCredential"
            },
            "description": "`credentials`, the name `auth/passkey/list/0.1` publishes. It was\n`passkeys` until #1112 — the same object under a name the schema does\nnot define, so no conforming client could find it."
          }
        }
      },
      "MemberEnvelope": {
        "type": "object",
        "description": "`{ member: … }` — the shape `vtc/members/show/0.1` publishes. The row was\nreturned bare until #1093; the row itself always conformed.",
        "required": [
          "member"
        ],
        "properties": {
          "member": {
            "$ref": "#/components/schemas/MemberResponse"
          }
        }
      },
      "MemberResponse": {
        "type": "object",
        "description": "Wire shape returned by both endpoints. Joins `members:<did>`\n+ `acl:<did>` so a caller doesn't need a second request.",
        "required": [
          "did",
          "role",
          "joinedAt",
          "publishConsent",
          "departurePreference",
          "extensions",
          "personhood"
        ],
        "properties": {
          "currentRoleVecId": {
            "type": [
              "string",
              "null"
            ]
          },
          "currentVmcId": {
            "type": [
              "string",
              "null"
            ]
          },
          "departurePreference": {
            "$ref": "#/components/schemas/Disposition"
          },
          "did": {
            "type": "string"
          },
          "extensions": {
            "$ref": "#/components/schemas/Value",
            "description": "Serialised as `{}` when the member has none.\n\nThe canonical component makes `extensions` **required** and types it\n`object`, so `null` is a violation and omitting it is a violation too —\nan empty object is the only conforming way to say \"none\". A stored\nmember that never had extensions holds `JsonValue::Null`, and this went\nout as `null` until #1107. The conformance fixture set\n`{\"org\": \"acme\"}`, so a hand-typed non-empty object hid it; the\nresponse-conformance layer on real traffic is what surfaced it."
          },
          "joinedAt": {
            "type": "string",
            "format": "date-time"
          },
          "joinedViaInvitation": {
            "type": "boolean",
            "description": "Whether the member auto-joined by presenting a verified\nInvitation Credential (VIC). Surfaced so the admin UI can\nbadge invitation-joined members."
          },
          "label": {
            "type": [
              "string",
              "null"
            ]
          },
          "memberVmcId": {
            "type": [
              "string",
              "null"
            ],
            "description": "Top-level `id` of the member-issued reciprocal VMC (member → community\nhalf of the pair), once the member has sent it over `members/vmc/1.0`.\n`None` until then. The VMC body itself is not echoed on this response."
          },
          "memberVmcReceivedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "When the member VMC was received + stored. Paired with\n[`Self::member_vmc_id`]."
          },
          "personhood": {
            "type": "boolean",
            "description": "Personhood flag (Phase 4 M4.1). Surfaces the Member row's\n`personhood` field. Read-only on this response —\n`POST /v1/members/{did}/personhood/assert` flips it (M4.3),\n`DELETE /v1/members/{did}/personhood` clears it (M4.4),\nand renewal-policy downgrade clears it (M4.2.2)."
          },
          "personhoodAssertedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time",
            "description": "Timestamp of the most recent assert. Operator-private —\nthe value is included on Admin-gated responses (this\nroute is `AdminAuth`) so operators can audit; the\npublic member-facing renewal response carries only the\n`personhood` flag."
          },
          "publishConsent": {
            "type": "boolean"
          },
          "role": {
            "$ref": "#/components/schemas/VtcRole"
          },
          "statusListIndex": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "minimum": 0
          }
        }
      },
      "Paginated_EndorsementRow": {
        "type": "object",
        "description": "Standard response wrapper for list endpoints. Carries the\nrequested page of items, the opaque cursor for the next page\n(`None` when the caller has reached the end), and an optional\ntotal-count estimate.",
        "required": [
          "items"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "One endorsement as the canonical `Endorsement` component names it.\n\nA wire type distinct from the stored [`Endorsement`] because renaming the\nstored fields would rewrite the persisted fjall rows — `Endorsement`\nderives `Deserialize` and serialises `camelCase`, so `id` /\n`endorsementType` / `createdAt` are the on-disk keys of every row already\nwritten. Mapping at the boundary keeps the wire correct without a data\nmigration, the same split `GenerationRow` uses (#1095).",
              "required": [
                "endorsementId",
                "typeUri",
                "subjectDid",
                "issued",
                "statusListIndex",
                "claim"
              ],
              "properties": {
                "claim": {
                  "$ref": "#/components/schemas/Value"
                },
                "endorsementId": {
                  "type": "string",
                  "format": "uuid",
                  "description": "`endorsementId`, stored as `id`."
                },
                "issued": {
                  "$ref": "#/components/schemas/CredentialReference",
                  "description": "A reference to the issued credential — **not** a timestamp.\n\n#1096 read `issued` as \"when it was issued\" and mapped `createdAt`\nonto it, so this went out as an RFC 3339 string where the component\nrequires an object."
                },
                "revokedAt": {
                  "type": [
                    "string",
                    "null"
                  ],
                  "format": "date-time"
                },
                "statusListIndex": {
                  "type": "integer",
                  "format": "int32",
                  "minimum": 0
                },
                "subjectDid": {
                  "type": "string"
                },
                "typeUri": {
                  "type": "string",
                  "description": "`typeUri`, stored as `endorsementType`."
                }
              }
            }
          },
          "nextCursor": {
            "type": [
              "string",
              "null"
            ]
          },
          "totalEstimate": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "minimum": 0
          }
        }
      },
      "Paginated_EndorsementType": {
        "type": "object",
        "description": "Standard response wrapper for list endpoints. Carries the\nrequested page of items, the opaque cursor for the next page\n(`None` when the caller has reached the end), and an optional\ntotal-count estimate.",
        "required": [
          "items"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "A registered endorsement type. Stored verbatim; the\nregistrar route enforces validation at insert time.",
              "required": [
                "typeUri",
                "createdAt",
                "createdByDid"
              ],
              "properties": {
                "claimSchema": {
                  "oneOf": [
                    {
                      "type": "null"
                    },
                    {
                      "$ref": "#/components/schemas/Value",
                      "description": "Optional JSON Schema for the claim body. Reserved for\nfuture per-type validation; the Phase 4 issuance path\nonly checks \"type is registered\" without consulting\nthe schema. Operators can read the schema from\n`GET /v1/endorsement-types/{uri}` and validate\nclient-side."
                    }
                  ]
                },
                "createdAt": {
                  "type": "string",
                  "format": "date-time"
                },
                "createdByDid": {
                  "type": "string",
                  "description": "Admin DID that registered the type. Carried for\naudit correlation against the\n`EndorsementTypeRegistered` envelope."
                },
                "description": {
                  "type": [
                    "string",
                    "null"
                  ],
                  "description": "Free-form description shown in admin UIs."
                },
                "typeUri": {
                  "type": "string",
                  "description": "The type URI. Primary key — URL-encoded into the\nkeyspace key."
                }
              }
            }
          },
          "nextCursor": {
            "type": [
              "string",
              "null"
            ]
          },
          "totalEstimate": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "minimum": 0
          }
        }
      },
      "Paginated_JoinRequest": {
        "type": "object",
        "description": "Standard response wrapper for list endpoints. Carries the\nrequested page of items, the opaque cursor for the next page\n(`None` when the caller has reached the end), and an optional\ntotal-count estimate.",
        "required": [
          "items"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "One join request. Stored under `join_requests:<id>`.",
              "required": [
                "id",
                "applicantDid",
                "vp",
                "submittedAt",
                "status"
              ],
              "properties": {
                "applicantDid": {
                  "type": "string"
                },
                "decision": {
                  "oneOf": [
                    {
                      "type": "null"
                    },
                    {
                      "$ref": "#/components/schemas/JoinDecision",
                      "description": "Why this request was refused, for the applicant.\n\nWritten by **both** rejection paths — the policy auto-deny at\nsubmit and the admin reject — so the status poll has one field\nto read rather than two shapes to reconcile. [`JoinDecision`]\nsays why that matters.\n\n`None` for a request that has not been rejected, and for one\nrejected before this field existed (see\n[`JoinRequest::decision_for_applicant`], which reconstructs what\nit can from [`Self::policy_decision`])."
                    }
                  ]
                },
                "extensions": {
                  "$ref": "#/components/schemas/Value",
                  "description": "Community-defined extensions slot (spec §3-M). Bounded by\n`JOIN_REQUEST_EXTENSIONS_MAX_BYTES` (16 KiB) at the route\nlayer.\nOmitted when null. The canonical `JoinRequest` component types this\n`object` and does not require it, so absent is how \"none\" is spelled\nand `null` is a type error — the same shape as `policyDecision` in\n#1099. Found by the response-conformance layer on real traffic; the\nfixture set a non-empty object and so never saw it."
                },
                "id": {
                  "type": "string",
                  "format": "uuid"
                },
                "policyDecision": {
                  "oneOf": [
                    {
                      "type": "null"
                    },
                    {
                      "$ref": "#/components/schemas/Value",
                      "description": "Set by Phase 2's policy step on approve / reject. Always\n`None` in Phase 1.\n\nOmitted rather than sent as `null`: the canonical `JoinRequest`\ncomponent types this `object`, not `[\"object\", \"null\"]` as it does\nthe neighbouring `vpClaims` and `decision`. It is not required, so\nabsent is the conforming way to say \"no policy decision\" — `null` is\na type error. It went out as `null` until #1099."
                    }
                  ]
                },
                "registryConsent": {
                  "type": "boolean",
                  "description": "Whether the applicant consents to being published in the\ncommunity's trust-registry record (spec §8). Default\n`false`; the operator-facing surface defers this decision\nto Phase 3."
                },
                "status": {
                  "$ref": "#/components/schemas/JoinStatus"
                },
                "submittedAt": {
                  "type": "string",
                  "format": "date-time"
                },
                "vp": {
                  "$ref": "#/components/schemas/Value",
                  "description": "Opaque VP carried verbatim from the wire. Phase 1 never\ninspects it beyond the holder-binding check the route layer\nran at submit time; Phase 2's policy step reads from\n[`Self::vp_claims`] (the canonical projection extracted at\nsubmit time), not from this field."
                },
                "vpClaims": {
                  "$ref": "#/components/schemas/Value",
                  "description": "Canonical projection of [`Self::vp`] computed at submit\ntime by [`crate::policy::extract::extract_vp_claims`] and\nfed to `join.rego` as `input.vp_claims`. Stored on the row\nso the approve flow doesn't have to re-extract (plan §D4).\n`null` on rows persisted before Phase 2."
                }
              }
            }
          },
          "nextCursor": {
            "type": [
              "string",
              "null"
            ]
          },
          "totalEstimate": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "minimum": 0
          }
        }
      },
      "Paginated_MemberResponse": {
        "type": "object",
        "description": "Standard response wrapper for list endpoints. Carries the\nrequested page of items, the opaque cursor for the next page\n(`None` when the caller has reached the end), and an optional\ntotal-count estimate.",
        "required": [
          "items"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "Wire shape returned by both endpoints. Joins `members:<did>`\n+ `acl:<did>` so a caller doesn't need a second request.",
              "required": [
                "did",
                "role",
                "joinedAt",
                "publishConsent",
                "departurePreference",
                "extensions",
                "personhood"
              ],
              "properties": {
                "currentRoleVecId": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "currentVmcId": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "departurePreference": {
                  "$ref": "#/components/schemas/Disposition"
                },
                "did": {
                  "type": "string"
                },
                "extensions": {
                  "$ref": "#/components/schemas/Value",
                  "description": "Serialised as `{}` when the member has none.\n\nThe canonical component makes `extensions` **required** and types it\n`object`, so `null` is a violation and omitting it is a violation too —\nan empty object is the only conforming way to say \"none\". A stored\nmember that never had extensions holds `JsonValue::Null`, and this went\nout as `null` until #1107. The conformance fixture set\n`{\"org\": \"acme\"}`, so a hand-typed non-empty object hid it; the\nresponse-conformance layer on real traffic is what surfaced it."
                },
                "joinedAt": {
                  "type": "string",
                  "format": "date-time"
                },
                "joinedViaInvitation": {
                  "type": "boolean",
                  "description": "Whether the member auto-joined by presenting a verified\nInvitation Credential (VIC). Surfaced so the admin UI can\nbadge invitation-joined members."
                },
                "label": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "memberVmcId": {
                  "type": [
                    "string",
                    "null"
                  ],
                  "description": "Top-level `id` of the member-issued reciprocal VMC (member → community\nhalf of the pair), once the member has sent it over `members/vmc/1.0`.\n`None` until then. The VMC body itself is not echoed on this response."
                },
                "memberVmcReceivedAt": {
                  "type": [
                    "string",
                    "null"
                  ],
                  "format": "date-time",
                  "description": "When the member VMC was received + stored. Paired with\n[`Self::member_vmc_id`]."
                },
                "personhood": {
                  "type": "boolean",
                  "description": "Personhood flag (Phase 4 M4.1). Surfaces the Member row's\n`personhood` field. Read-only on this response —\n`POST /v1/members/{did}/personhood/assert` flips it (M4.3),\n`DELETE /v1/members/{did}/personhood` clears it (M4.4),\nand renewal-policy downgrade clears it (M4.2.2)."
                },
                "personhoodAssertedAt": {
                  "type": [
                    "string",
                    "null"
                  ],
                  "format": "date-time",
                  "description": "Timestamp of the most recent assert. Operator-private —\nthe value is included on Admin-gated responses (this\nroute is `AdminAuth`) so operators can audit; the\npublic member-facing renewal response carries only the\n`personhood` flag."
                },
                "publishConsent": {
                  "type": "boolean"
                },
                "role": {
                  "$ref": "#/components/schemas/VtcRole"
                },
                "statusListIndex": {
                  "type": [
                    "integer",
                    "null"
                  ],
                  "format": "int32",
                  "minimum": 0
                }
              }
            }
          },
          "nextCursor": {
            "type": [
              "string",
              "null"
            ]
          },
          "totalEstimate": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "minimum": 0
          }
        }
      },
      "Paginated_Relationship": {
        "type": "object",
        "description": "Standard response wrapper for list endpoints. Carries the\nrequested page of items, the opaque cursor for the next page\n(`None` when the caller has reached the end), and an optional\ntotal-count estimate.",
        "required": [
          "items"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "A stored, verified VRC. Field order matches the spec §5.4\nsurface (issuer/subject DIDs + the credential body).",
              "required": [
                "id",
                "issuerDid",
                "subjectDid",
                "vrcJsonld",
                "vrcDigestMultibase",
                "createdAt"
              ],
              "properties": {
                "createdAt": {
                  "type": "string",
                  "format": "date-time"
                },
                "id": {
                  "type": "string",
                  "format": "uuid",
                  "description": "Server-allocated UUID. Surfaced as `urn:uuid:<id>` on\nthe VRC's top-level `id` field at publish time when\nthe caller didn't supply one."
                },
                "issuerDid": {
                  "type": "string",
                  "description": "The asserting member. By the publish-time auth check,\nthis equals the caller's session DID."
                },
                "lifecycle": {
                  "$ref": "#/components/schemas/LifecycleLog",
                  "description": "Everything recorded against this edge since it was published:\nsuspension, restoration, supersession, withdrawal.\n\nEmpty on every edge published before #1079 and on every edge that has\nhad no lifecycle event, which is why it is `#[serde(default)]` and\nskipped when empty — existing rows decode unchanged and re-serialise\nbyte-identically, the same treatment [`Relationship::persona`] got when\nit was added to an already-shipped row.\n\nThis is the *only* lifecycle handle a VRC has. Per planning-review D7 a\nVRC carries no `credentialStatus`, deliberately — a status-list host\nlearns which verifier checked which credential and when, which is the\ncorrelation the pairwise work exists to remove. Without a status list\nthe community's alternatives were a permanent edge or `DELETE\n/v1/relationships/{id}`, and a deletion cannot answer \"was this edge in\nforce last Tuesday\" because it can only be observed as an absence.\n\nNever resolved by hand. [`Relationship::in_force_at`] is the entry\npoint, and the precedence rule lives in\n[`crate::credentials::lifecycle`].\n\n**Known spec lag.** `Relationship` is both the stored row and the item\nshape of `spec/vtc/relationships/list/0.2`, whose response schema is\n`additionalProperties: false` and predates this member — as it predates\n[`Relationship::persona`], added the same way in #1067. A listed edge\nthat has had a lifecycle event therefore carries a member the published\ntask does not describe. The registry is upstream and the spec moves\nfirst, so this is recorded here rather than papered over by dropping\nthe field on the way out: hiding the log from the one surface a member\ncan read would leave them able to be suspended and unable to see it."
                },
                "persona": {
                  "oneOf": [
                    {
                      "type": "null"
                    },
                    {
                      "$ref": "#/components/schemas/PersonaAnnotation",
                      "description": "The persona (VPC) this edge's issuer has asserted on it,\nif any. Absent on every edge published before #1067 and\non every edge whose issuer has not asserted a persona —\nwhich is why it is `#[serde(default)]`: existing rows\ndecode unchanged.\n\nA VPC is an *annotation* credential (DTG Credentials\n§Annotation Credentials): it creates no graph structure,\nit attaches to structure that already exists. Storing it\non the edge row rather than in a keyspace of its own is\nthe direct expression of that — there is no persona\nrecord without an edge to hang it on, and deleting the\nedge deletes the annotation with it."
                    }
                  ]
                },
                "subjectDid": {
                  "type": "string",
                  "description": "The other party the VRC names. Need not be a current\ncommunity member at *list* time (the list path strips\nPurge-removed rows per §12.3) but must be a current\nmember at *publish* time (default `relationships.rego`)."
                },
                "vrcDigestMultibase": {
                  "type": "string",
                  "description": "SHA-256 of `canonical_json(vrc_jsonld)`, hex-encoded.\nUsed for idempotency: a second publish of an\nalready-stored VRC returns the existing id (200)\nrather than creating a duplicate row.\nDigest over the RFC 8785 canonicalization of `vrc_jsonld`, as a\nbase58btc multibase multihash — the form DTG Credentials and\n`relationships/publish/0.2` both specify.\n\nWas a bare lowercase-hex SHA-256 over a recursive key sort. That form\nhard-coded one algorithm into the stored shape, named no base encoding,\nand — worse — named no canonicalization a second implementation could\nreproduce, so two conforming services could digest the same credential\ndifferently and neither be wrong."
                },
                "vrcJsonld": {
                  "$ref": "#/components/schemas/Value",
                  "description": "The VRC body verbatim — JSON-LD, including the\ndata-integrity proof. Stored as `JsonValue` rather\nthan a typed `VerifiableCredential` so future VRC\nshape extensions don't require a storage migration."
                }
              }
            }
          },
          "nextCursor": {
            "type": [
              "string",
              "null"
            ]
          },
          "totalEstimate": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "minimum": 0
          }
        }
      },
      "PasskeyLoginFinishRequest": {
        "type": "object",
        "description": "`POST /v1/auth/passkey-login/finish`.\n\nVerifies the WebAuthn assertion, looks up the registered\nadmin DID by credential ID, and mints the cookie session.\nSets the same `vtc_admin_session` + `csrf` cookies as\n`admin_session` does for the bearer-token bridge. Returns the\nbearer token in the body for clients that want to also use it\nprogrammatically.",
        "required": [
          "auth_id",
          "credential"
        ],
        "properties": {
          "auth_id": {
            "type": "string"
          },
          "credential": {
            "type": "object"
          }
        }
      },
      "PasskeyLoginStartRequest": {
        "type": "object",
        "description": "Request body for `passkey-login/start`, per\n`spec/auth/passkey/login/start/0.2`.\n\nEntirely optional — the admin SPA's login posts no body at all, which reads\nas `purpose: login`, the pre-existing behaviour.",
        "properties": {
          "ext": {
            "type": "object",
            "description": "Vendor-namespaced extensions per SPEC.md §4.5.1. Accepted and ignored,\nwhich is what the framework asks of a consumer that defines no\nextensions — the field exists so `deny_unknown_fields` doesn't reject a\nspec-conformant producer that sends one."
          },
          "purpose": {
            "type": "string",
            "description": "`login` (default) issues a new session; `stepUp` elevates the caller's\nexisting one. The two differ in more than bookkeeping: a step-up must be\nauthenticated, and challenges **only the caller's own** credentials, so\nanother admin's passkey cannot satisfy it."
          },
          "subject": {
            "type": [
              "string",
              "null"
            ],
            "description": "The VID the producer intends to authenticate as. Optional — omit for\nthe usernameless / discoverable-credential flow the admin SPA uses.\n\nWhen given it is **honoured, not noted**: a login challenge is narrowed\nto that subject's credentials, and a step-up that names anyone other\nthan the authenticated session's subject is refused rather than quietly\nanswered for the session holder."
          }
        },
        "additionalProperties": false
      },
      "PasskeyLoginStartResponse": {
        "type": "object",
        "description": "`POST /v1/auth/passkey-login/start`.\n\nBrowser-friendly login: the admin SPA submits no body, the\ndaemon returns a WebAuthn assertion challenge across every\nregistered passkey (discoverable login — the user picks their\ndevice, the browser chooses the matching credential). Modelled\non `affinidi-webvh-service::login_start`.\n\nUnauthenticated by design: the eventual `finish` ceremony\nproves possession of an enrolled credential, which is the auth.\n\nThe same canonical task also serves **AAL step-up**, selected by\n[`purpose`](PasskeyLoginStartRequest::purpose) — see that field.",
        "required": [
          "authId",
          "options"
        ],
        "properties": {
          "authId": {
            "type": "string"
          },
          "options": {
            "type": "object",
            "description": "The value for `navigator.credentials.get({ publicKey: … })` — the\ninner options, not webauthn-rs's `{publicKey: …}` wrapper. See\n`admin::passkeys::RegisterStartResponse::options` for why the wrapper\nwent."
          }
        }
      },
      "PatchRequest": {
        "type": "object",
        "description": "PATCH request body: a `key → value` map under `overrides`. Keys not in\n[`crate::config_store::REGISTRY`] are reported back under\n`rejected` rather than silently dropped.\n\nThe map is wrapped (rather than `#[serde(flatten)]`ed to the top level) to\nmatch canonical `spec/config/patch/0.1`, whose envelope is\n`additionalProperties: false` around a single `overrides` object.",
        "required": [
          "overrides"
        ],
        "properties": {
          "overrides": {
            "type": "object",
            "additionalProperties": {},
            "propertyNames": {
              "type": "string"
            }
          }
        },
        "additionalProperties": false
      },
      "PatchResponse": {
        "type": "object",
        "description": "PATCH response body. Lists which keys took effect immediately,\nwhich await restart, and which were rejected.",
        "required": [
          "applied",
          "pendingRestart",
          "rejected"
        ],
        "properties": {
          "applied": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "pendingRestart": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "rejected": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RejectedKey"
            }
          }
        }
      },
      "PersonaAnnotation": {
        "type": "object",
        "description": "A Verifiable Persona Credential (VPC) attached to one edge.\n\nThe VPC is issued by the *edge issuer* under a persona DID\n(P-DID) and names the edge's counterparty as its subject. It\nis the sanctioned mechanism for deliberate correlation: a\nmember who wants several of their pairwise edges to be\nrecognisable as one party asserts the same P-DID on each,\nwithout ever putting their membership DID in a credential.\n\nSee `docs/05-design-notes/vpc-persona-annotation.md` for how\nthe annotation is bound to the edge, and for what upstream\n(trustoverip/dtgwg-cred-spec#9) has not yet settled.",
        "required": [
          "personaDid",
          "vpcJsonld",
          "attachedAt"
        ],
        "properties": {
          "attachedAt": {
            "type": "string",
            "format": "date-time"
          },
          "personaDid": {
            "type": "string",
            "description": "The VPC's `issuer` — the P-DID. Denormalised out of\n`vpc_jsonld` so the graph view can group edges by persona\nwithout re-parsing every credential."
          },
          "vpcJsonld": {
            "$ref": "#/components/schemas/Value",
            "description": "The VPC body verbatim, including its data-integrity\nproof, so a consumer of the row can re-verify the\nassertion rather than taking the VTC's word for it."
          }
        }
      },
      "PersonaResponse": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "personaDid": {
            "type": [
              "string",
              "null"
            ],
            "description": "The P-DID now on the edge, or `null` after a detach."
          }
        }
      },
      "PersonhoodGovernance": {
        "type": "object",
        "description": "What a community's governance asserts about personhood, and therefore\nwhether its VMCs may be read as PHCs.\n\nDTG Credentials §Personhood Credentials: \"A PHC is simply a VMC issued by\na VTC whose governance enforces: real human personhood; exactly one\nmembership per person.\"  Those two clauses are the two booleans here.\n\n## Both halves, separately\n\nThey are separate fields because they are separately achievable, and a\ncommunity that has one without the other should be able to say so. This\ndaemon's in-person vetting supports the first — an administrator meets\nsomeone and issues them an identity-verification endorsement — and does\nnothing at all for the second. Collapsing them into one `is_phc` flag\nwould force every such community to either overclaim or stay silent.\n\n## Declaration, not enforcement — and the honesty rule that follows\n\nLike [`CommunityProfile::relationship_identifier_default`], this describes\nwhat the community's governance requires. Nothing here makes the daemon\nenforce anything.\n\nThat makes an overclaim worse than silence. Before this field, a verifier\nreading a `PersonhoodCredential` type had no authoritative source and knew\nit; a `singleMembership: true` from a community that does not enforce\nuniqueness gives that verifier a false one. **Do not set a flag the\ncommunity's governance does not actually require** — and note that\nrequiring it is not the same as this daemon checking it. `personhood.rego`\nis where a requirement becomes a gate.",
        "properties": {
          "acceptedIdvps": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "DIDs of the identity-verification providers whose credentials this\ncommunity accepts as personhood evidence.\n\n§Governance Considerations item 1: identity-proofing requirements\n\"including acceptable IDVPs and IDVCs\" are the community's to define\nand are \"published via trust registries\". A community that vets its\nown members in person lists its own C-DID here — it is acting as its\nown IDVP, which §IDVC permits.\n\nAdvisory to verifiers, not a gate: `personhood.rego` decides what is\nactually accepted. An empty list means the community has not published\none, not that it accepts everything."
          },
          "governanceFrameworkUrl": {
            "type": [
              "string",
              "null"
            ],
            "description": "Where the governance framework this all refers to can be read.\n\nThe ToIP Governance Metamodel the spec cites expects a human-readable\ndocument behind these assertions. The booleans above are the\nmachine-readable summary; this is the thing they summarise."
          },
          "realHuman": {
            "type": "boolean",
            "description": "Governance requires that members are real humans.\n\nDefaults to `false`: a community that has not considered the question\nasserts nothing, which is the only safe default for a claim a verifier\nmay rely on."
          },
          "singleMembership": {
            "type": "boolean",
            "description": "Governance requires that each person holds at most one membership in\n**this** community.\n\nPer-community by definition — the spec's glossary says \"exactly one\nmembership in that VTC\" — so a single community can satisfy this\nwithout any network above it.\n\nThis is the half that needs an anchor the community cannot supply\nitself: nothing in the credential graph distinguishes one person with\ntwo DIDs from two people. See `docs/03-vtc/personhood-and-graph.md`."
          }
        }
      },
      "PolicyGetResponse": {
        "type": "object",
        "description": "Canonical `policy/get` response.",
        "required": [
          "policy"
        ],
        "properties": {
          "policy": {
            "$ref": "#/components/schemas/PolicyModuleResponse"
          }
        }
      },
      "PolicyListResponse": {
        "type": "object",
        "description": "Canonical `policy/list` response.",
        "required": [
          "policies",
          "truncated"
        ],
        "properties": {
          "cursor": {
            "type": [
              "string",
              "null"
            ]
          },
          "policies": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PolicyModuleResponse"
            }
          },
          "truncated": {
            "type": "boolean",
            "description": "Canonical-required: more matching modules exist beyond this page."
          }
        }
      },
      "PolicyModuleResponse": {
        "type": "object",
        "description": "Canonical `policy/_shared` **PolicyModule**.\n\nField mapping from VTC's storage row:\n- `rego_source` → `module`\n- `version` (monotone per-purpose revision counter) → `version`,\n  which canonical also uses as the optimistic-concurrency token.\n- `updatedAt` is the activation time when the row has been\n  activated, else its creation time. A VTC revision is immutable, so\n  activation is the only thing that can change it after write.\n- `sha256` / `author-did` / the intrinsic `purpose` have no canonical\n  home and ride in `ext` (the canonical type is\n  `additionalProperties: false`). Every `ext` key is spelled in the\n  lowercase-and-hyphen form SPEC §4.5.1 requires — a capital letter makes\n  the key itself invalid, which is a framework violation rather than a\n  style preference.\n\n`appliesTo` / `priority` / `enabled` are deliberately **not**\nemitted: VTC does no `appliesTo`/`priority` selection, and emitting\nempty values would imply a selection model that isn't there.",
        "required": [
          "id",
          "name",
          "module",
          "version",
          "createdAt",
          "updatedAt",
          "ext"
        ],
        "properties": {
          "createdAt": {
            "type": "string"
          },
          "ext": {},
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "module": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "updatedAt": {
            "type": "string"
          },
          "version": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          }
        }
      },
      "PolicyPurpose": {
        "type": "string",
        "description": "What a policy decides. The keyspace `active_policies:<purpose>`\nhas at most one row per variant; the active pointer is a\nper-purpose singleton.\n\nWire shape is `camelCase` ASCII (`join`, `removal`,\n`crossCommunityRoles`) — operators wire purposes into REST\npayloads + the policies CLI verbs.\n\nPer spec §7.1, the workspace ships nine purposes. They split\ninto three groups:\n- **Membership lifecycle**: [`Self::Join`], [`Self::Removal`],\n  [`Self::Personhood`].\n- **Discoverability**: [`Self::Registry`], [`Self::Directory`].\n- **Authorization**: [`Self::RoleDefinitions`],\n  [`Self::CrossCommunityRoles`],\n  [`Self::CrossCommunityRelationships`], [`Self::Relationships`].",
        "enum": [
          "join",
          "removal",
          "personhood",
          "registry",
          "directory",
          "roleDefinitions",
          "crossCommunityRoles",
          "crossCommunityRelationships",
          "relationships",
          "roleChange"
        ]
      },
      "PolicyStatusFilter": {
        "type": "string",
        "enum": [
          "active",
          "archived"
        ]
      },
      "ProfileWithStatus": {
        "allOf": [
          {
            "$ref": "#/components/schemas/CommunityProfile"
          },
          {
            "type": "object",
            "required": [
              "registryStatus"
            ],
            "properties": {
              "registryStatus": {
                "$ref": "#/components/schemas/HealthStatus",
                "description": "Trust-registry reachability — `\"active\"` when the last\nhealth probe succeeded, `\"degraded\"` otherwise (probe\nnever ran, last probe failed, or `registry.url` is\nunset). Spec §8.1."
              }
            }
          }
        ],
        "description": "The persisted profile plus the one field that is computed per request.\n\n`registryStatus` sits *inside* the profile object rather than beside it,\nbecause that is where the canonical `CommunityProfile` component defines\nit (\"Populated on reads; not settable\"). Un-flattening alone would have\nleft it a sibling of `profile`, which the response schema forbids."
      },
      "PublicCommunityProfile": {
        "type": "object",
        "description": "Public read shape — the curated subset of [`CommunityProfile`]\nexposed unauthenticated at `GET /v1/community/public-profile`.\n\nDrops `extensions` (operator-defined JSON, not guaranteed\npublic-safe) and `registryStatus` (operational telemetry that\nbelongs behind admin auth). Adds `mediator_did` so the default\nlanding page can render the community's full identity in one\nfetch.",
        "required": [
          "communityDid",
          "name",
          "description",
          "language",
          "createdAt",
          "personhood",
          "transports"
        ],
        "properties": {
          "communityDid": {
            "type": "string"
          },
          "contactEmail": {
            "type": [
              "string",
              "null"
            ]
          },
          "createdAt": {
            "type": "string",
            "format": "date-time"
          },
          "description": {
            "type": "string"
          },
          "language": {
            "type": "string"
          },
          "logoUrl": {
            "type": [
              "string",
              "null"
            ]
          },
          "mediatorDid": {
            "type": [
              "string",
              "null"
            ],
            "description": "The community's DIDComm mediator DID, if one is configured.\nSourced from the live daemon config (same as `/health`), not\nthe persisted profile row."
          },
          "name": {
            "type": "string"
          },
          "personhood": {
            "$ref": "#/components/schemas/PersonhoodGovernance",
            "description": "What this community's governance asserts about personhood.\n\nPublished **unauthenticated, on purpose**. DTG Credentials puts PHC\nstatus in \"governance and trust registries, not credential structure\",\nso the party who needs this is a verifier holding one of this\ncommunity's VMCs — someone who is not a member, has no token, and may\nhave no relationship with the community at all. Behind auth it would\nanswer the question only for people who did not need to ask it.\n\nThis is the *community's own claim about itself*. A verifier deciding\nhow much it is worth reads the trust registry, which is what the spec\ncalls authoritative; this is the self-published copy, and it is here so\nthe claim is resolvable at all rather than so it is trusted."
          },
          "publicUrl": {
            "type": [
              "string",
              "null"
            ]
          },
          "transports": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TransportStatus"
            },
            "description": "How to reach this community, and whether each route currently works.\n\nEvery protocol is listed, each carrying `advertised` (the DID document\noffers it) and `serviceable` (this VTC can answer on it right now).\nReachable means both.\n\n**The DID document remains authoritative.** This is a *view* of it,\nresolved at request time, published so a visitor or a monitor can\nvalidate connectivity without resolving the DID themselves — never an\nindependent declaration. A client choosing a transport must still match\non the document's service `type`; if the two ever disagree, the document\nwins and this field is the thing that is wrong.\n\nEmpty when the community's own DID could not be resolved — reported as\nunknown rather than as \"advertises nothing\", which would be a lie a\ncaller might act on."
          }
        }
      },
      "PublishBody": {
        "type": "object",
        "required": [
          "vrc"
        ],
        "properties": {
          "pop": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Value",
                "description": "Proof that the caller controls the key behind the VRC's\n`issuer`, when that is not the caller's session DID —\ni.e. whenever the credential is issued under a pairwise\nrelationship DID rather than the member's membership DID.\n\nSee [`verify_publish_authorization`]. Required unless\n`issuer` equals the session DID (the deprecated form)."
              }
            ]
          },
          "vrc": {
            "$ref": "#/components/schemas/Value",
            "description": "The self-issued VRC, in the DTG Credentials wire form:\n`@context` carrying the W3C v2 and DTG contexts, `type`\n`[\"VerifiableCredential\", \"DTGCredential\",\n\"RelationshipCredential\"]`, an `issuer`,\n`credentialSubject.id` naming the subject, and a\ndata-integrity proof. Built by\n`dtg_credentials::DTGCredential::new_vrc`."
          }
        }
      },
      "PublishResponse": {
        "type": "object",
        "required": [
          "id",
          "issuerDid",
          "subjectDid",
          "vrcDigestMultibase"
        ],
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "issuerDid": {
            "type": "string"
          },
          "subjectDid": {
            "type": "string"
          },
          "vrcDigestMultibase": {
            "type": "string"
          }
        }
      },
      "RecogniseChallengeResponse": {
        "type": "object",
        "description": "Response body for `POST /v1/auth/recognise/challenge`.",
        "required": [
          "nonce",
          "expiresAt"
        ],
        "properties": {
          "expiresAt": {
            "type": "integer",
            "format": "int64",
            "description": "Unix-epoch seconds at which the challenge expires.",
            "minimum": 0
          },
          "nonce": {
            "type": "string",
            "description": "Single-use nonce the holder must bind into the VP's top-level `nonce`."
          }
        }
      },
      "RecogniseData": {
        "type": "object",
        "required": [
          "accessToken",
          "accessExpiresAt",
          "foreignIssuerDid",
          "mappedRole"
        ],
        "properties": {
          "accessExpiresAt": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "accessToken": {
            "type": "string",
            "description": "Minted JWT. Carries the *mapped local* role, not the\nforeign one."
          },
          "foreignIssuerDid": {
            "type": "string",
            "description": "Foreign issuer DID, surfaced so the caller can correlate\nthe response with their request (the route doesn't echo\nthe credentials)."
          },
          "mappedRole": {
            "type": "string",
            "description": "Local role the foreign role mapped to."
          }
        }
      },
      "RecogniseRequest": {
        "type": "object",
        "description": "Request body for `POST /v1/auth/recognise`. The caller supplies a\nholder-signed W3C Verifiable Presentation that embeds the foreign VEC and\nVMC in `verifiableCredential` and binds the challenge `nonce` (top-level)\nplus this VTC's DID as the `domain`. The route verifies the holder proof,\nthe embedded issuer proofs, the status list, and the registry recognition\nitself.",
        "required": [
          "presentation"
        ],
        "properties": {
          "presentation": {
            "$ref": "#/components/schemas/Value",
            "description": "A W3C Data-Integrity VP, holder-signed with\n`proofPurpose: authentication`."
          }
        }
      },
      "RecogniseResponse": {
        "type": "object",
        "required": [
          "sessionId",
          "data"
        ],
        "properties": {
          "data": {
            "$ref": "#/components/schemas/RecogniseData"
          },
          "sessionId": {
            "type": "string"
          }
        }
      },
      "RecognitionCheck": {
        "type": "object",
        "required": [
          "did",
          "recognised",
          "registryConfigured"
        ],
        "properties": {
          "did": {
            "type": "string",
            "description": "Echo of the queried DID."
          },
          "error": {
            "type": [
              "string",
              "null"
            ],
            "description": "Set when the registry query failed (unreachable / parse) rather than\nreturning a clean recognised/not-recognised verdict."
          },
          "recognised": {
            "type": "boolean",
            "description": "Whether this community recognises the DID (TRQP `recognise`). `false`\nwhen no registry is configured or the query came back not-found."
          },
          "registryConfigured": {
            "type": "boolean",
            "description": "Whether a trust registry is configured at all (no-registry mode → every\nforeign DID is unrecognised)."
          }
        }
      },
      "RegisterAcceptsBody": {
        "type": "object",
        "required": [
          "id",
          "query"
        ],
        "properties": {
          "description": {
            "type": [
              "string",
              "null"
            ]
          },
          "id": {
            "type": "string"
          },
          "query": {
            "$ref": "#/components/schemas/Value"
          }
        }
      },
      "RegisterBody": {
        "type": "object",
        "required": [
          "typeUri"
        ],
        "properties": {
          "claimSchema": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Value"
              }
            ]
          },
          "description": {
            "type": [
              "string",
              "null"
            ]
          },
          "typeUri": {
            "type": "string"
          }
        }
      },
      "RegisterFinishRequest": {
        "type": "object",
        "required": [
          "registration_id",
          "register_response",
          "uv_response",
          "label"
        ],
        "properties": {
          "label": {
            "type": "string",
            "description": "Operator-supplied label (e.g. `\"YubiKey 5C\"`)."
          },
          "register_response": {
            "type": "object"
          },
          "registration_id": {
            "type": "string"
          },
          "transports": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "uv_response": {
            "type": "object"
          }
        }
      },
      "RegisterFinishResponse": {
        "type": "object",
        "required": [
          "credentialId",
          "subject",
          "registeredAt"
        ],
        "properties": {
          "credentialId": {
            "type": "string"
          },
          "registeredAt": {
            "type": "string",
            "format": "date-time",
            "description": "When the credential was registered. Required by the task, and the same\ninstant recorded on the stored `RegisteredPasskey`, so a client need\nnot re-list to learn it."
          },
          "subject": {
            "type": "string",
            "description": "The DID this passkey now authenticates.\n\nRequired by `auth/passkey/enroll/finish/0.2` and absent until #1112.\nA caller enrolling on behalf of someone else had no way to confirm\n*whose* credential it just created — the response named the credential\nand not the subject, which is the half that matters for an audit trail."
          }
        }
      },
      "RegisterResponse": {
        "type": "object",
        "description": "`{ endorsementType: … }` — the shape `vtc/endorsement-types/register/0.1`\npublishes. The handler returned the bare row until #1059's witness\ncompared it with its own schema; the row itself conformed.",
        "required": [
          "endorsementType"
        ],
        "properties": {
          "endorsementType": {
            "$ref": "#/components/schemas/EndorsementType"
          }
        }
      },
      "RegisterSchemaBody": {
        "type": "object",
        "required": [
          "typeUri",
          "kind"
        ],
        "properties": {
          "credentialSchema": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Value"
              }
            ]
          },
          "description": {
            "type": [
              "string",
              "null"
            ]
          },
          "dtgType": {
            "type": [
              "string",
              "null"
            ]
          },
          "kind": {
            "$ref": "#/components/schemas/SchemaKind"
          },
          "typeUri": {
            "type": "string"
          }
        }
      },
      "RegisterStartResponse": {
        "type": "object",
        "required": [
          "enrollmentId",
          "options",
          "uvOptions"
        ],
        "properties": {
          "enrollmentId": {
            "type": "string",
            "description": "Opaque id the operator passes back to `enroll/finish`.\n\n`enrollmentId`, the name the task publishes — it was `registrationId`\nuntil #1112."
          },
          "options": {
            "type": "object",
            "description": "The value for `navigator.credentials.create({ publicKey: … })`.\n\nThe **inner** options, not webauthn-rs's `{publicKey: …}` wrapper.\n`CredentialCreationOptions` in the published schema describes itself as\n\"server-issued options for `navigator.credentials.create({ publicKey:\n... })`\" — the value that goes *inside* the key, not the object\ncontaining it.\n\nThe wrapper was pure ceremony: the admin SPA unwrapped it with\n`start.registerOptions.publicKey` and immediately re-wrapped it as\n`create({ publicKey: … })`. Sending the inner object means the client\nwrites `create({ publicKey: start.options })` and nothing is undone on\nthe way."
          },
          "uvOptions": {
            "type": "object",
            "description": "The value for `navigator.credentials.get({ publicKey: … })` for the\nstep-up assertion against an existing passkey. Unwrapped for the same\nreason as `options`."
          }
        }
      },
      "RegisteredCredential": {
        "type": "object",
        "description": "The wire view of a passkey, as `auth/passkey/list/0.1` publishes it.\n\nSeparate from [`RegisteredPasskey`] because that type is a **storage row**:\nit is `Deserialize`d out of the `admin:<did>` record, so its member names\nare the on-disk format and renaming them would orphan every passkey already\nenrolled. The two shapes disagree in exactly the two ways the schema cares\nabout, and both differences are the storage row's to keep:\n\n- `label` is published as `deviceLabel`, and is **absent** rather than empty\n  when nobody chose one. The schema is explicit that a consumer must not\n  synthesize a label, \"because an invented one is indistinguishable from a\n  chosen one to somebody deciding which credential to revoke\" — and `\"\"` is\n  an invented one.\n- `lastUsedAt` is omitted when unset. Sending `null` is a type error against\n  a `date-time` member, and the schema notes it deliberately does not\n  distinguish \"never used\" from \"not tracked\", since no binding could carry\n  the difference anyway.",
        "required": [
          "credentialId",
          "transports",
          "registeredAt"
        ],
        "properties": {
          "credentialId": {
            "type": "string"
          },
          "deviceLabel": {
            "type": [
              "string",
              "null"
            ]
          },
          "lastUsedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time"
          },
          "registeredAt": {
            "type": "string",
            "format": "date-time"
          },
          "transports": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      },
      "RegistryTransport": {
        "type": "object",
        "description": "How the VTC reaches its trust registry, as an operator sees it.\n\nThe two protocol fields answer different questions, and reporting only one\nof them is what made the old `registry_status` misleading:\n\n- `advertised` — what the registry's **DID document** says it speaks. Read\n  from the document, which is the authority (CLAUDE.md), so an operator can\n  see the registry's own claim without resolving the DID by hand.\n- `active` — what the last selection actually **chose**, which is the\n  highest-preference protocol present in both parties' documents. Empty\n  until the first call; `error` says why if selection failed.\n\nAdvertised without active means the two sides have no transport in common,\nor nothing has tried yet — a distinction a single \"protocol\" field cannot\nexpress, and precisely the state that leaves a registry looking configured\nwhile nothing can reach it.",
        "required": [
          "advertised"
        ],
        "properties": {
          "active": {
            "type": [
              "string",
              "null"
            ],
            "description": "The protocol the last selection chose. `None` before the first call, or\nwhen selection failed — see `error`."
          },
          "advertised": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Protocols the registry's DID document advertises, in preference order\n(`tsp`, `didcomm`, `rest`). Empty when the DID has not been resolved\nyet, or when the registry is addressed by URL alone."
          },
          "did": {
            "type": [
              "string",
              "null"
            ],
            "description": "The registry's DID, when it is addressed by one."
          },
          "error": {
            "type": [
              "string",
              "null"
            ],
            "description": "Why the last selection failed, if it did."
          },
          "url": {
            "type": [
              "string",
              "null"
            ],
            "description": "The registry's REST base URL, when one is configured."
          }
        }
      },
      "RejectedKey": {
        "type": "object",
        "description": "One rejected key + the reason. Surfaced to the caller so the\nadmin UX can present a meaningful error inline.",
        "required": [
          "key",
          "reason"
        ],
        "properties": {
          "key": {
            "type": "string"
          },
          "reason": {
            "type": "string"
          }
        }
      },
      "Relationship": {
        "type": "object",
        "description": "A stored, verified VRC. Field order matches the spec §5.4\nsurface (issuer/subject DIDs + the credential body).",
        "required": [
          "id",
          "issuerDid",
          "subjectDid",
          "vrcJsonld",
          "vrcDigestMultibase",
          "createdAt"
        ],
        "properties": {
          "createdAt": {
            "type": "string",
            "format": "date-time"
          },
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "Server-allocated UUID. Surfaced as `urn:uuid:<id>` on\nthe VRC's top-level `id` field at publish time when\nthe caller didn't supply one."
          },
          "issuerDid": {
            "type": "string",
            "description": "The asserting member. By the publish-time auth check,\nthis equals the caller's session DID."
          },
          "lifecycle": {
            "$ref": "#/components/schemas/LifecycleLog",
            "description": "Everything recorded against this edge since it was published:\nsuspension, restoration, supersession, withdrawal.\n\nEmpty on every edge published before #1079 and on every edge that has\nhad no lifecycle event, which is why it is `#[serde(default)]` and\nskipped when empty — existing rows decode unchanged and re-serialise\nbyte-identically, the same treatment [`Relationship::persona`] got when\nit was added to an already-shipped row.\n\nThis is the *only* lifecycle handle a VRC has. Per planning-review D7 a\nVRC carries no `credentialStatus`, deliberately — a status-list host\nlearns which verifier checked which credential and when, which is the\ncorrelation the pairwise work exists to remove. Without a status list\nthe community's alternatives were a permanent edge or `DELETE\n/v1/relationships/{id}`, and a deletion cannot answer \"was this edge in\nforce last Tuesday\" because it can only be observed as an absence.\n\nNever resolved by hand. [`Relationship::in_force_at`] is the entry\npoint, and the precedence rule lives in\n[`crate::credentials::lifecycle`].\n\n**Known spec lag.** `Relationship` is both the stored row and the item\nshape of `spec/vtc/relationships/list/0.2`, whose response schema is\n`additionalProperties: false` and predates this member — as it predates\n[`Relationship::persona`], added the same way in #1067. A listed edge\nthat has had a lifecycle event therefore carries a member the published\ntask does not describe. The registry is upstream and the spec moves\nfirst, so this is recorded here rather than papered over by dropping\nthe field on the way out: hiding the log from the one surface a member\ncan read would leave them able to be suspended and unable to see it."
          },
          "persona": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/PersonaAnnotation",
                "description": "The persona (VPC) this edge's issuer has asserted on it,\nif any. Absent on every edge published before #1067 and\non every edge whose issuer has not asserted a persona —\nwhich is why it is `#[serde(default)]`: existing rows\ndecode unchanged.\n\nA VPC is an *annotation* credential (DTG Credentials\n§Annotation Credentials): it creates no graph structure,\nit attaches to structure that already exists. Storing it\non the edge row rather than in a keyspace of its own is\nthe direct expression of that — there is no persona\nrecord without an edge to hang it on, and deleting the\nedge deletes the annotation with it."
              }
            ]
          },
          "subjectDid": {
            "type": "string",
            "description": "The other party the VRC names. Need not be a current\ncommunity member at *list* time (the list path strips\nPurge-removed rows per §12.3) but must be a current\nmember at *publish* time (default `relationships.rego`)."
          },
          "vrcDigestMultibase": {
            "type": "string",
            "description": "SHA-256 of `canonical_json(vrc_jsonld)`, hex-encoded.\nUsed for idempotency: a second publish of an\nalready-stored VRC returns the existing id (200)\nrather than creating a duplicate row.\nDigest over the RFC 8785 canonicalization of `vrc_jsonld`, as a\nbase58btc multibase multihash — the form DTG Credentials and\n`relationships/publish/0.2` both specify.\n\nWas a bare lowercase-hex SHA-256 over a recursive key sort. That form\nhard-coded one algorithm into the stored shape, named no base encoding,\nand — worse — named no canonicalization a second implementation could\nreproduce, so two conforming services could digest the same credential\ndifferently and neither be wrong."
          },
          "vrcJsonld": {
            "$ref": "#/components/schemas/Value",
            "description": "The VRC body verbatim — JSON-LD, including the\ndata-integrity proof. Stored as `JsonValue` rather\nthan a typed `VerifiableCredential` so future VRC\nshape extensions don't require a storage migration."
          }
        }
      },
      "RelationshipsGraph": {
        "type": "object",
        "required": [
          "nodes",
          "edges"
        ],
        "properties": {
          "edges": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/GraphEdge"
            }
          },
          "nodes": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/GraphNode"
            }
          }
        }
      },
      "ReloadResponse": {
        "type": "object",
        "description": "`POST /v1/admin/config/reload` response. Lists the keys whose\n**db-layer** values were applied in-memory by this call. Keys\nflagged `requires_restart` never appear here — they re-apply on\nthe next restart.",
        "required": [
          "keysReloaded"
        ],
        "properties": {
          "keysReloaded": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      },
      "RemoveBody": {
        "type": "object",
        "properties": {
          "disposition": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Disposition"
              }
            ]
          },
          "reason": {
            "type": [
              "string",
              "null"
            ],
            "description": "Optional admin-only reason. Self-remove ignores this (the\nmember doesn't need to justify their own departure). Capped\nat 1024 chars at the route layer."
          }
        }
      },
      "RemoveResponse": {
        "type": "object",
        "required": [
          "did",
          "disposition",
          "removed"
        ],
        "properties": {
          "did": {
            "type": "string"
          },
          "disposition": {
            "type": "string"
          },
          "removed": {
            "type": "boolean"
          }
        }
      },
      "RemovedMember": {
        "type": "object",
        "description": "A departed member whose Member row was retained (Tombstone / Historical\ndisposition) after its ACL was deleted. Surfaced so operators can see who\nleft and purge the lingering rows.",
        "required": [
          "did",
          "removedAt",
          "status"
        ],
        "properties": {
          "did": {
            "type": "string"
          },
          "removedAt": {
            "type": "string",
            "format": "date-time",
            "description": "When the member departed."
          },
          "status": {
            "type": "string",
            "description": "Always `\"removed\"` — present so the UI can render a uniform status."
          },
          "statusListIndex": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "description": "Revocation slot the member held (for audit / re-flip), if any.",
            "minimum": 0
          }
        }
      },
      "RemovedMembersResponse": {
        "type": "object",
        "description": "`{ removed: [...] }` — the shape `vtc/members/removed/0.1` publishes.\n\nThe handler returned a top-level array until #1059's witness compared it\nwith its schema. The rows themselves always conformed.",
        "required": [
          "removed"
        ],
        "properties": {
          "removed": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RemovedMember"
            }
          }
        }
      },
      "RenewResponse": {
        "type": "object",
        "required": [
          "did",
          "vmc",
          "roleVec",
          "personhood",
          "personhoodChanged"
        ],
        "properties": {
          "did": {
            "type": "string"
          },
          "personhood": {
            "type": "boolean",
            "description": "`personhood.rego` re-eval outcome for the new VMC.\nPhase 2's deny-all default keeps this `false`; the\nfield exists from day one so Phase 4's\nassert/revoke endpoints don't break the wire shape."
          },
          "personhoodChanged": {
            "type": "boolean",
            "description": "`true` when the personhood flag flipped from the prior\nVMC. Surfaced separately from `personhood` itself so\ncallers can light up a \"your personhood status\nchanged\" notification."
          },
          "roleVec": {
            "$ref": "#/components/schemas/Value"
          },
          "vmc": {
            "$ref": "#/components/schemas/Value"
          }
        }
      },
      "RequestVmcBody": {
        "type": "object",
        "properties": {
          "reason": {
            "type": [
              "string",
              "null"
            ],
            "description": "Optional operator note (\"renewal\", \"audit\", …) relayed to the member."
          }
        }
      },
      "RequestVmcResponse": {
        "type": "object",
        "required": [
          "memberDid",
          "requested",
          "threadId"
        ],
        "properties": {
          "memberDid": {
            "type": "string"
          },
          "requested": {
            "type": "boolean",
            "description": "Always `true` — the request was dispatched to the member's agent. The\nmember replies asynchronously over `members/vmc/1.0`."
          },
          "threadId": {
            "type": "string",
            "description": "DIDComm thread id of the dispatched request, for correlation."
          }
        }
      },
      "RestartResponse": {
        "type": "object",
        "description": "`POST /v1/admin/config/restart` response when the supervisor\ncheck passes.",
        "required": [
          "supervisor",
          "drainTimeoutSeconds"
        ],
        "properties": {
          "drainTimeoutSeconds": {
            "type": "integer",
            "format": "int64",
            "description": "`drain_timeout` (seconds) the daemon will use for graceful\nshutdown. Mirrors `RestartRequestedData.drain_timeout_seconds`.",
            "minimum": 0
          },
          "supervisor": {
            "$ref": "#/components/schemas/SupervisorKind",
            "description": "Which supervisor the daemon detected (so the operator's\nadmin UX can echo it back)."
          }
        }
      },
      "RevocationDetail": {
        "type": "object",
        "description": "The credential the revocation applies to, and when it took effect.",
        "required": [
          "credentialId",
          "revokedAt"
        ],
        "properties": {
          "credentialId": {
            "type": "string"
          },
          "revokedAt": {
            "type": "string"
          }
        }
      },
      "RevokeBody": {
        "type": "object",
        "properties": {
          "pop": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Value",
                "description": "Proof that the caller controls the key behind the row's `issuerDid`,\nwhen that is not the caller's session DID — i.e. for every edge\npublished under a pairwise relationship DID.\n\nBound to the row `id` rather than to a credential, because `DELETE\n/v1/relationships/{id}` names the row and carries no credential to\nbind to."
              }
            ]
          }
        }
      },
      "RevokeByDidResponse": {
        "type": "object",
        "required": [
          "revoked"
        ],
        "properties": {
          "revoked": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          }
        }
      },
      "RevokeFinishRequest": {
        "type": "object",
        "required": [
          "revocation_id",
          "uv_response"
        ],
        "properties": {
          "revocation_id": {
            "type": "string"
          },
          "uv_response": {
            "type": "object"
          }
        }
      },
      "RevokeFinishResponse": {
        "type": "object",
        "required": [
          "credentialId",
          "revokedAt",
          "remaining"
        ],
        "properties": {
          "credentialId": {
            "type": "string"
          },
          "remaining": {
            "type": "integer",
            "description": "How many passkeys the subject has left.\n\nRequired by `auth/passkey/revoke/finish/0.1` and absent until #1112.\nIt is the number the caller most needs and the one this endpoint is\nuniquely placed to give: the last-passkey guard refuses to leave an\nadmin at zero, so a client that cannot see the count cannot tell\nwhether the next revoke will succeed without attempting it.",
            "minimum": 0
          },
          "revokedAt": {
            "type": "string",
            "format": "date-time",
            "description": "When the credential was revoked."
          }
        }
      },
      "RevokeInviteResponse": {
        "type": "object",
        "required": [
          "jti"
        ],
        "properties": {
          "jti": {
            "type": "string"
          }
        }
      },
      "RevokeResponse": {
        "type": "object",
        "required": [
          "endorsementId",
          "revocation",
          "statusListIndex"
        ],
        "properties": {
          "endorsementId": {
            "type": "string"
          },
          "revocation": {
            "$ref": "#/components/schemas/RevocationDetail"
          },
          "statusListIndex": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          }
        }
      },
      "RevokeStartRequest": {
        "type": "object",
        "required": [
          "credential_id"
        ],
        "properties": {
          "credential_id": {
            "type": "string"
          }
        }
      },
      "RevokeStartResponse": {
        "type": "object",
        "required": [
          "revocationId",
          "uvOptions"
        ],
        "properties": {
          "revocationId": {
            "type": "string"
          },
          "uvOptions": {
            "type": "object",
            "description": "Unwrapped, as everywhere else in this family — the value for\n`navigator.credentials.get({ publicKey: … })`."
          }
        }
      },
      "SchemaEntry": {
        "type": "object",
        "description": "One registered credential-type schema in the community's schema store.",
        "required": [
          "typeUri",
          "kind",
          "createdAt",
          "createdByDid"
        ],
        "properties": {
          "createdAt": {
            "type": "string",
            "format": "date-time"
          },
          "createdByDid": {
            "type": "string",
            "description": "Admin DID that registered the schema (audit correlation)."
          },
          "credentialSchema": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Value",
                "description": "The JSON Schema (W3C `credentialSchema`) an issued/accepted credential\nmust conform to. Validated at issue time (task 2.3). `None` means\n\"registered, but no schema constraint\"."
              }
            ]
          },
          "description": {
            "type": [
              "string",
              "null"
            ],
            "description": "Free-form description shown in admin UIs."
          },
          "dtgType": {
            "type": [
              "string",
              "null"
            ],
            "description": "The DTG catalog type this binds to (a `DTGCredentialType` string, e.g.\n`\"MembershipCredential\"`). `None` for community-defined endorsement types\nthat map onto the generic `EndorsementCredential`."
          },
          "kind": {
            "$ref": "#/components/schemas/SchemaKind",
            "description": "Issues (the VTC mints it) or Accepts (recognised as evidence)."
          },
          "typeUri": {
            "type": "string",
            "description": "The credential type URI / `vct`. Primary key — URL-encoded into the key."
          }
        }
      },
      "SchemaKind": {
        "type": "string",
        "description": "Whether a registered schema describes a credential type the community\n**Issues** (mints) or **Accepts** (recognises as evidence).",
        "enum": [
          "issues",
          "accepts"
        ]
      },
      "SendQueryRequest": {
        "type": "object",
        "description": "`POST /v1/join-requests/query` request — ask the VTC to prepare a join query\nfor `holderDid` from the named Accepts criterion.",
        "required": [
          "holderDid",
          "criterionId"
        ],
        "properties": {
          "criterionId": {
            "type": "string",
            "description": "The registered Accepts criterion whose DCQL query to send."
          },
          "holderDid": {
            "type": "string",
            "description": "The holder the query is for (delivered to it over DIDComm by the VTC or a\nrelayer — relayer ≠ holder is supported; the holder still proves binding)."
          }
        }
      },
      "SendQueryResponse": {
        "type": "object",
        "description": "`POST /v1/join-requests/query` response — the prepared query to deliver, and\nthe thread the holder presents on.",
        "required": [
          "threadId",
          "holderDid",
          "query",
          "delivered"
        ],
        "properties": {
          "delivered": {
            "type": "boolean",
            "description": "Whether the VTC **accepted the query for guaranteed delivery** (durably\nqueued to the outbox; the drain loop sends + retries it) — not a\nconfirmation that it has been received. `false` when no mediator is\nconfigured or the enqueue failed — the caller can still relay the returned\n[`query`](Self::query) out-of-band (relayer ≠ holder). The wire field name\nstays `delivered` (camelCase, `deny_unknown_fields`) for API stability."
          },
          "holderDid": {
            "type": "string",
            "description": "Echoed for the caller's correlation."
          },
          "query": {
            "type": "object",
            "description": "The `credential-exchange/query` body to deliver to the holder."
          },
          "threadId": {
            "type": "string",
            "description": "The DIDComm thread id the holder must reply on (the challenge is keyed by\nit; the `present` handler consumes it)."
          }
        }
      },
      "Session": {
        "type": "object",
        "description": "Canonical `Session` from `spec/auth/_shared/0.1/session.schema.json`.\n\nAligns with OIDC Core §2 / RFC 8176:\n- `amr`: authentication method references. VTI vocabulary uses\n  `\"did\"` (challenge-response), `\"passkey\"` (WebAuthn assertion),\n  `\"vta\"` (verifiable-trust-agent approval).\n- `acr`: authentication context class reference. Typical values\n  `\"aal1\"` (single-factor DID), `\"aal2\"` (second possession/\n  biometric factor), `\"aal3\"` (hardware-bound second factor).",
        "required": [
          "id",
          "subject",
          "issuedAt",
          "expiresAt"
        ],
        "properties": {
          "acr": {
            "type": "string"
          },
          "amr": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "expiresAt": {
            "type": "string",
            "description": "ISO-8601 timestamp the session ceases to be valid."
          },
          "id": {
            "type": "string"
          },
          "issuedAt": {
            "type": "string",
            "description": "ISO-8601 timestamp the session was created."
          },
          "subject": {
            "type": "string"
          }
        }
      },
      "SessionState": {
        "type": "string",
        "description": "Session lifecycle state.",
        "enum": [
          "ChallengeSent",
          "Authenticated"
        ]
      },
      "SessionSummary": {
        "type": "object",
        "required": [
          "sessionId",
          "did",
          "state",
          "createdAt"
        ],
        "properties": {
          "createdAt": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "did": {
            "type": "string"
          },
          "refreshExpiresAt": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "minimum": 0
          },
          "sessionId": {
            "type": "string"
          },
          "state": {
            "$ref": "#/components/schemas/SessionState"
          }
        }
      },
      "SessionView": {
        "type": "object",
        "description": "The canonical `Session` shape.",
        "required": [
          "id",
          "subject",
          "issuedAt",
          "expiresAt"
        ],
        "properties": {
          "acr": {
            "type": [
              "string",
              "null"
            ],
            "description": "Authentication context class per OIDC Core §2, when recorded."
          },
          "amr": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Authentication methods per RFC 8176, when the token records any."
          },
          "expiresAt": {
            "type": "string",
            "format": "date-time"
          },
          "id": {
            "type": "string"
          },
          "issuedAt": {
            "type": "string",
            "format": "date-time",
            "description": "When the session was issued — the JWT's `iat`, required by the\ncomponent and simply never surfaced before."
          },
          "subject": {
            "type": "string",
            "description": "The DID this session authenticates."
          }
        }
      },
      "ShowWhen": {
        "type": "object",
        "description": "A `showWhen` predicate, declarative so it crosses the wire: render\nthe field only when `field`'s value equals `eq` (or its truthiness\nmatches `truthy`).",
        "required": [
          "field"
        ],
        "properties": {
          "eq": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Value"
              }
            ]
          },
          "field": {
            "type": "string"
          },
          "truthy": {
            "type": [
              "boolean",
              "null"
            ]
          }
        }
      },
      "SupervisorKind": {
        "type": "string",
        "enum": [
          "manual",
          "systemd",
          "kubernetes"
        ]
      },
      "TestBody": {
        "type": "object",
        "required": [
          "query",
          "input"
        ],
        "properties": {
          "input": {
            "$ref": "#/components/schemas/Value",
            "description": "JSON document fed to the policy as `input`. Mirrors the\nshape M2.6 / M2.7 will pass in production."
          },
          "query": {
            "type": "string",
            "description": "Rego query to evaluate against the candidate policy\n(e.g. `\"data.vtc.join.allow\"`). Caller chooses the query so\n`test` can be used to probe any rule in the module, not\njust `allow`."
          }
        }
      },
      "TestResponse": {
        "type": "object",
        "required": [
          "id",
          "purpose",
          "sha256",
          "result"
        ],
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "purpose": {
            "$ref": "#/components/schemas/PolicyPurpose"
          },
          "result": {
            "$ref": "#/components/schemas/Value",
            "description": "Raw regorus `QueryResults` JSON. Same shape M2.6 / M2.7\nwill pluck `result[0].expressions[0].value` from when they\nwire policy evaluation into the membership flows."
          },
          "sha256": {
            "type": "string"
          }
        }
      },
      "TokenBundle": {
        "type": "object",
        "description": "Canonical `TokenBundle` from `spec/auth/_shared/0.1/tokens.schema.json`.\n\nOAuth 2.0 (RFC 6749 §5.1)-shaped: `expiresIn` is seconds from\nissuance, not an absolute timestamp. Clients compute the absolute\nexpiry as `now() + expires_in` immediately after issuance, or\nstore the issuance moment alongside the bundle.",
        "required": [
          "accessToken",
          "tokenType",
          "expiresIn"
        ],
        "properties": {
          "accessToken": {
            "type": "string"
          },
          "expiresIn": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "refreshExpiresIn": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "minimum": 0
          },
          "refreshToken": {
            "type": [
              "string",
              "null"
            ]
          },
          "scope": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "tokenType": {
            "type": "string"
          }
        }
      },
      "TransportStatus": {
        "type": "object",
        "description": "One transport's public connectivity status, as reported on the\nunauthenticated community profile.\n\nThe two flags answer different questions and both are needed:\n\n- `advertised` — will a client resolving this community's DID *find* this\n  transport? Read from the DID document, which is the authority (CLAUDE.md).\n- `serviceable` — if reached on it, can this VTC actually answer? Build\n  capability plus live messaging connectivity.\n\nA transport is genuinely reachable only when **both** are true. Splitting\nthem is the point: the failure that motivated all of this was `advertised`\nwithout `serviceable`, and a single boolean would have hidden it exactly the\nway the original defect did.",
        "required": [
          "protocol",
          "advertised",
          "serviceable"
        ],
        "properties": {
          "advertised": {
            "type": "boolean",
            "description": "Present in the community's DID document, so a resolving client will\nfind it."
          },
          "endpoint": {
            "type": [
              "string",
              "null"
            ],
            "description": "The advertised endpoint: the mediator DID for TSP/DIDComm, the base URL\nfor REST. `None` when not advertised — there is no endpoint to give."
          },
          "protocol": {
            "type": "string",
            "description": "`\"tsp\"` / `\"didcomm\"` / `\"rest\"`."
          },
          "serviceable": {
            "type": "boolean",
            "description": "This VTC can answer on it right now. For the messaging transports that\nmeans the build supports the protocol *and* the mediator connection is\nlive — a re-falsifiable signal, never a boot-time latch (R6.2)."
          }
        }
      },
      "UpdateAclRequest": {
        "type": "object",
        "description": "Canonical `acl/change-role` request.\n\nRole-only, and `fromRole` is a **compare-and-swap guard**, not\ndecoration: the maintainer must confirm the subject's current role\nequals it and refuse otherwise. That closes the read-modify-write\nrace the previous partial update had — two admins demoting the same\nsubject concurrently could each read `admin` and write a different\nresult, last-writer-wins, with no signal.\n\nLabel and scope edits are **not** here: they go to `acl/grant` with\nthe subject's existing role, which is what canonical means by \"the\nentry the maintainer should hold\".",
        "required": [
          "fromRole",
          "toRole"
        ],
        "properties": {
          "fromRole": {
            "$ref": "#/components/schemas/VtcRole"
          },
          "reason": {
            "type": [
              "string",
              "null"
            ],
            "description": "Operator rationale. Emitted on the service log line for this\nchange; the audit envelope's data types do not carry a free-text\nreason today, so this is deliberately not described as audited."
          },
          "toRole": {
            "$ref": "#/components/schemas/VtcRole"
          }
        },
        "additionalProperties": false
      },
      "UpdateMemberRequest": {
        "type": "object",
        "description": "Body of the PATCH request. Every field is optional; a request\nwith no fields is a no-op (200 with the current row).",
        "properties": {
          "departurePreference": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Disposition"
              }
            ]
          },
          "extensions": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Value"
              }
            ]
          },
          "label": {
            "type": [
              "string",
              "null"
            ],
            "description": "Human-readable name for this member, shown wherever their DID is\nrendered (admin UI, `vtc acl list`).\n\nThe label lives on the ACL row, so until now it was writable only via\n`acl/grant` — a whole re-grant to correct a typo in a display name.\nAn empty string clears it; omitting the field leaves it unchanged."
          },
          "publishConsent": {
            "type": [
              "boolean",
              "null"
            ]
          },
          "role": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/VtcRole"
              }
            ]
          }
        }
      },
      "UpdateProfileResponse": {
        "type": "object",
        "description": "PUT response shape — echoes the updated profile + the list of\nfields that actually changed (operator-friendly + powers the\naudit event emitter that lands alongside this in a follow-up).",
        "required": [
          "profile",
          "fieldsChanged"
        ],
        "properties": {
          "fieldsChanged": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "profile": {
            "$ref": "#/components/schemas/CommunityProfile"
          }
        }
      },
      "UploadBody": {
        "type": "object",
        "required": [
          "name",
          "module"
        ],
        "properties": {
          "appliesTo": {
            "type": [
              "array",
              "null"
            ],
            "items": {
              "type": "string"
            }
          },
          "description": {
            "type": [
              "string",
              "null"
            ]
          },
          "enabled": {
            "type": [
              "boolean",
              "null"
            ]
          },
          "expectedVersion": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "description": "Optimistic-concurrency token. When present it MUST equal the\ncurrent highest revision for this purpose, else the caller is\nwriting over a revision it never saw.",
            "minimum": 0
          },
          "ext": {
            "description": "Ecosystem extension members. MUST carry\n`org.openvtc.purpose` — see [`crate::routes::policies::read::PURPOSE_EXT_KEY`]\nfor why purpose is intrinsic here."
          },
          "id": {
            "type": [
              "string",
              "null"
            ],
            "format": "uuid",
            "description": "Canonical members this maintainer does not implement. Present\nso they are *refused* rather than silently dropped — a caller\nthat sets `enabled: false` must not have it ignored."
          },
          "module": {
            "type": "string",
            "description": "Full Rego source — canonical `module`. Bounded by\n[`POLICY_SOURCE_MAX_BYTES`]; uploads above the cap are\nrejected with 413."
          },
          "name": {
            "type": "string",
            "description": "Human-readable module name (canonical-required)."
          },
          "priority": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          }
        },
        "additionalProperties": false
      },
      "UploadResponse": {
        "type": "object",
        "required": [
          "policy",
          "created"
        ],
        "properties": {
          "created": {
            "type": "boolean",
            "description": "Canonical-required: true when this call created a new module\nlineage rather than revising an existing one. VTC's first\nrevision for a purpose is a creation."
          },
          "policy": {
            "$ref": "#/components/schemas/PolicyModuleResponse"
          }
        }
      },
      "Value": {},
      "VerifyResponse": {
        "type": "object",
        "description": "Result of a chain verification pass.",
        "required": [
          "verified",
          "entriesExamined",
          "entriesVerified",
          "legacySkipped",
          "unparseableSkipped"
        ],
        "properties": {
          "chainBreak": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ChainBreakReport",
                "description": "Where the chain broke. Absent when `verified` is true."
              }
            ]
          },
          "entriesExamined": {
            "type": "integer",
            "description": "Envelopes examined, chainable or not.",
            "minimum": 0
          },
          "entriesVerified": {
            "type": "integer",
            "description": "Envelopes that carried a chain link and verified.",
            "minimum": 0
          },
          "ext": {
            "description": "Signed-checkpoint verification (#708) — the half of this endpoint that\na store-level adversary cannot satisfy.\n\nCarried under `ext` since #1110. The canonical response is\n`additionalProperties: false` and defines no checkpoint member, so a\ntop-level one made every verify response non-conformant. This is\ngenuinely load-bearing evidence rather than a nicety, so it is not\ndropped — `ext` is the framework's sanctioned slot for exactly this,\nand the member is worth proposing upstream so it need not live in an\nextension at all."
          },
          "head": {
            "type": [
              "string",
              "null"
            ],
            "description": "Head of the verified chain, hex-encoded. `None` when nothing\nchainable was found."
          },
          "legacySkipped": {
            "type": "integer",
            "description": "Pre-v2 envelopes skipped as unchainable.\n\n**Non-zero is a finding on a store that should hold none.**\n`verify_chain` skips these rows rather than verifying them, so\nthey are an insertion point: an envelope forged with\n`schemaVersion: 1` passes untouched.",
            "minimum": 0
          },
          "unparseableSkipped": {
            "type": "integer",
            "description": "Rows that would not deserialize into an envelope at all. Also\nskipped, and also a finding — reported separately from\n`legacySkipped` because the cause differs (corruption or a\nforward-version row, versus a pre-chain row).",
            "minimum": 0
          },
          "verified": {
            "type": "boolean",
            "description": "Whether every chainable envelope verified."
          }
        }
      },
      "VtcRole": {
        "type": "string",
        "description": "One of `admin`, `moderator`, `issuer`, `member`, or                  `custom:<name>` where `<name>` is 1..=64 lowercase                  alphanumerics, `-`, or `_`.",
        "examples": [
          "admin"
        ]
      },
      "WhoamiResponse": {
        "type": "object",
        "description": "Wire shape returned by `whoami`. Minimal: enough for the admin\nSPA's nav header to show \"Signed in as …\" with a role badge,\nwithout needing to decode the JWT client-side (the session\ncookie is HttpOnly).",
        "required": [
          "session",
          "roles",
          "scopes"
        ],
        "properties": {
          "roles": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The caller's roles. Plural because the component says so: a single\nrole is one entry, and a deployment that grows to several does not\nneed a new response shape."
          },
          "scopes": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The contexts this session may act in — `allowedContexts` under the\ncanonical name."
          },
          "session": {
            "$ref": "#/components/schemas/SessionView",
            "description": "The caller's session, under the member the task publishes.\n\nThis response was a flat `{did, role, sessionId, accessExpiresAt,\nallowedContexts}` until #1112 — every member outside the canonical\nvocabulary, on a response that is `additionalProperties: false`. No\nconforming client could read any of it."
          }
        }
      }
    },
    "securitySchemes": {
      "bearer_jwt": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    }
  }
}
